/**
 * Replaced by the build and test configurations. This module is internal and
 * is not exported from the package public API.
 */
declare const __ERPBRIDGE_SDK_VERSION__: string

export const SDK_VERSION: string = __ERPBRIDGE_SDK_VERSION__
