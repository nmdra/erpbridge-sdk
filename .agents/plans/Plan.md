# Plan: ERPBridge SDK (TypeScript) — v0.1.0

## Goal

Ship a **TypeScript client library** (`@erpbridge/sdk`) that gives application and AI-agent developers a single, ergonomic way to consume an ERPBridge server — MCP tool calls, REST tool invocation, log aggregation, metrics, cache, and health — **without hand-rolling JSON-RPC, SSE parsing, or retry logic**. Authentication support ships in a separate future plan (see D17) once the server's own auth lands.

For whom: developers embedding ERPBridge into agentic-AI workflows, web backends, and monitoring tooling (the audience the docs site already targets in `docs/sdk/overview.mdx`).

Why: today the only options are bridgectl (a CLI) or copy-pasting the hand-rolled client examples in `docs/erpbridge/mcp-client-guide.mdx`. Neither provides a library that handles typed results, session reconnects, and typed REST wrappers.

Decisions confirmed with user: **TypeScript** (v1), **wrap the official MCP client** (`@modelcontextprotocol/client@~2.0.0`, aligned to the server's mcp-go spec), **auth deferred to a future plan**, REST surface = **logs + metrics + health + cache + tool registry + direct invoke**, repo at **`~/Documents/Projects/erpbridge-sdk`**.

## Current State

Evidence gathered from the three project repos (server, docs, prototype):

- **Server** (`~/Documents/Projects/ERPBridge`, Go, mcp-go v0.57.0):
  - MCP streamable HTTP at `/mcp/` (stateful, session idle TTL 30 min) + stdio; `initialize`/`tools/list`/`tools/call` lifecycle (`internal/mcp/server.go:465-551`). Tools are registered **bare-named** (`list_employees`), NOT `erp.`-prefixed; the `erp.` prefix in AGENTS.md is not implemented (`internal/mcp/server.go:404`).
  - REST surface (docs/api.md): `POST /api/tools/invoke` `{name, arguments}` → `{result, error?, isError?}` (500 on tool error); `/apis/erpbridge.io/v1/tools` GET/POST/DELETE (registry CRUD); `/api/logs/recent` (JSON array, last 1000) and `/api/logs/stream` (SSE `data: <json>\n\n`); `/api/cache/stats` + `/api/cache/flush?tool&module&all`; `/mcp/health` → `{"status":"ok"}`; `/metrics` (Prometheus text).
  - **Auth is planned, not shipped**: `.agents/plans/Plan.md` A1–A10 all unchecked. Intended design (docs/erpbridge/auth.mdx:62-74): `API_AUTH_TOKEN` admin env var; `erpbt_`-prefixed API tokens, SHA-256-hashed in SQLite; scopes `mcp|metrics|logs`; CRUD at `/apis/erpbridge.io/v1/tokens`; bearer enforcement only when `API_AUTH_TOKEN` is set (open-mode back-compat). CORS on `/mcp/` **already allows** the `Authorization` header (`internal/mcp/server.go:472`), so the SDK can send tokens today.
  - Metric names (internal/metrics/metrics.go:8-69): `erp_requests_total{method,path,status}`, `erp_request_duration_seconds{method,path}`, `mcp_tool_invocations_total{tool,cache_status}`, `mcp_tool_duration_seconds{tool}`, `cache_hits_total{type}`, `cache_misses_total`, `mcp_sessions_active`, `mcp_server_starts_total`, `mcp_server_stops_total`, `mcp_sessions_started_total`, `mcp_sessions_ended_total`.
  - Log record fields (internal/logger + middleware.go:83-91): `level`, `msg`, `time`, `component`, `session_id`, `request_id`, `tool_name` (JSON, redacted).
- **Docs site** (`~/Documents/Projects/erpbridge-docs`, Docusaurus 3 / TS, Node >= 20): `docs/sdk/` section, `sdkSidebar`, navbar "SDK" item, and footer link **already registered** (`docs/sdk/overview.mdx` is a WIP stub listing exactly: built-in MCP client, log aggregation, view metrics). New pages dropped into `docs/sdk/` appear automatically (autogenerated sidebar, sidebars.ts:80). Adding SDK docs needs **no config changes**.
- **Prototype** (`~/Documents/Projects/erpbridge-client-test/erpbridge-client`): a scratch demo script (official Python `mcp` SDK + ollama chat loop). Not a library; no auth, no REST wrapping. Informative only — not reused.

### External research (TS SDK practices, 2025–2026, verified 2026-08-20)

- **Build tooling**: tsdown (Rolldown-based) is the fast-growing successor to tsup and is what the official MCP TypeScript SDK v2 builds with (`format: ['esm','cjs']` + `fixedExtension`, PR #2405); tsup remains the battle-tested fallback with a compatible API in v0.22.x (PkgPulse comparison, pkgpulse.com/guides/tsup-vs-tsdown-vs-unbuild-typescript-library-bundling-2026). tsdown auto-generates `exports` and integrates publint/attw validation (tsdown.dev/guide).
- **MCP SDK split (v2)**: `@modelcontextprotocol/sdk` v1 is monolithic — the client drags in server deps (express, hono, cors, npm registry). The v2 line splits into `@modelcontextprotocol/client`, `@modelcontextprotocol/server`, `@modelcontextprotocol/core`; `client@2.0.0` is client-only, Node >= 20, ESM-first + CJS, deps: zod, jose, cross-spawn, eventsource, eventsource-parser, pkce-challenge (npm registry, checked 2026-08-20).
- **Dual ESM/CJS**: canonical 2026 manifest = `"type": "module"` + `exports` with `types` nested first in each `import`/`require` branch, `.mjs`/`.cjs` + `.d.mts`/`.d.cts`, legacy `main`/`module`/`types` fallbacks (Node docs nodejs.org/api/packages.html; MCP client package.json as the working example). One shared `.d.ts` for both branches leaves CJS consumers wrong-typed under `node16` (javascript-package-publishing.com). `require(esm)` stable in Node 25.4.0 (2026-01) makes ESM-only defensible later, but dual remains the enterprise-safe default (SnipShift 2026-07).
- **Types/config**: author for `module/moduleResolution: nodenext` (the only correct module options for Node libs — typescriptlang.org modules reference); `verbatimModuleSyntax` + `isolatedModules` (+ `isolatedDeclarations` TS 5.5+) are the recommended library flags; run `tsc --noEmit` as a separate typecheck gate (js-package-publishing).
- **Source maps**: ship `declarationMap` **only if `src/` ships too** (go-to-def lands on missing files otherwise); plain `.js.map` is bloat for unminified output (web-standards.dev 2026-08).
- **Metadata**: `files` allowlist (`["dist","src"]`), `sideEffects: false` only when truly side-effect-free, SPDX `license`, `engines` advisory, `./package.json` exported for tooling (npm docs; webpack tree-shaking guide).
- **Subpath exports** are the SDK norm (MCP v2 ships `.`/`./stdio`/`./validators/*` with a runtime-neutral root — ts.sdk.modelcontextprotocol.io/v2/get-started/packages.html). Cautionary tale: MCP SDK v1.29.0's exports map pointed at files missing from the tarball, breaking root import (issue #2273) — tarball-level validation is mandatory.
- **Versioning/release**: for a standalone package with 1–2 maintainers, **release-please** (PR-driven, human gate, composes with npm publish) beats changesets (monorepo-first by design — changesets/docs/decisions.md) and semantic-release (no review step, tag on every merge) (latchkey.dev, tenki.cloud 2026 comparisons).
- **npm publishing**: **Trusted Publishing (OIDC) is GA since 2025-07-31** — no `NODE_AUTH_TOKEN`, provenance automatic for public repos; requires npm >= 11.5.1, Node >= 22.14, GitHub-hosted runners, and the package to already exist on npmjs.com; `repository.url` must exactly match the GitHub repo (github.blog changelog 2025-07-31; docs.npmjs.com/trusted-publishers/).
- **CI shape**: Node LTS matrix (`lts/-2`…`lts/*`), `npm ci`, separate lint/typecheck/test/build jobs; top-level `permissions: contents: read`; SHA-pin all third-party actions (GitHub's allowed-actions policy can block unpinned actions since 2025-08-15); Dependabot for `github-actions` + `npm`; no caching in release builds (docs.github.com secure-use; npm trusted-publishers example).
- **Publish gates**: `npm pack --dry-run --json` file assertions + **publint** + **attw --pack** (they catch different failure classes — publint.dev/docs/comparisons) + `npm audit --audit-level=high` AND **osv-scanner** (different advisory feeds; union is the finding set) — on PRs and a scheduled nightly scan (google/osv-scanner-action).
- **Docs deploy**: official chain `actions/configure-pages` → `upload-pages-artifact` → `deploy-pages` on the `github-pages` environment with `pages: write` + `id-token: write`; separate workflow from release; not a `gh-pages` branch push (docs.github.com Pages custom workflows; k0swe PR #820).

## Decisions

| #   | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Rationale / rejected                                                                                                                                                                                                                                                                                                                                                           |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D1  | **Language & packaging**: TypeScript, strict mode, ESM + CJS dual build via **tsdown** (Rolldown-based, the tool the MCP TypeScript SDK itself builds with), Node >= 20, package name `@erpbridge/sdk`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Matches docs site (TS); agentic-AI ecosystem is JS/TS-heavy. Python/Go deferred to a follow-up release (user-confirmed). tsdown chosen over tsup (battle-tested fallback, tsup-compatible API in v0.22.x) for Rolldown momentum, auto-`exports` generation, and built-in publint/attw validation (tsdown.dev/guide; MCP SDK builds with tsdown, PR #2405).                     |
| D2  | **MCP core**: wrap official `@modelcontextprotocol/client@~2.0.0` (client-only split package) `Client` + `StreamableHTTPClientTransport`. **Version aligned to the server's spec**: the server runs mcp-go v0.57.0 with `LATEST_PROTOCOL_VERSION = "2025-11-25"` and negotiation down to `2025-03-26`; `@modelcontextprotocol/core@2.0.0` exposes the identical set (`LATEST = "2025-11-25"`, `DEFAULT_NEGOTIATED = "2025-03-26"`, supported `[2025-11-25, 2025-06-18, 2025-03-26, 2024-11-05, 2024-10-07]` — verified in the published bundle). `~2.0.0` pins the 2.0.x surface for v0.1.0 stability; Dependabot PRs surface 2.1+ for human review. Re-evaluate when the server's future `Plan-MCP-Upgrade.md` (mcp-go v1.0.0-beta.1, spec 2026-07-28) lands. | Rejected hand-rolled JSON-RPC (docs guide's style is pedagogical only) and rejected v1 `@modelcontextprotocol/sdk`: it pulls server deps (express, hono, cors) we never use; the v2 `@modelcontextprotocol/client` is client-only, Node >= 20, ESM-first + CJS (npm registry, checked 2026-08-20).                                                                             |
| D3  | **Auth is deferred — v1 ships unauthenticated**: no token resolution, no bearer injection, no `ERPBridge_TOKEN` handling, no no-token warning in v1. The SDK connects anonymously and surfaces server 401s as-is. Auth (bearer resolution/injection, 401 mapping, scope awareness) is a separate **future plan** (`Plan-auth.md`), enabled only once the server's own auth (`ERPBridge/.agents/plans/Plan-Auth.md` A1–A10, currently all unchecked) ships.                                                                                                                                                                                                                                                                                                     | Server auth is unimplemented (verified: no `API_AUTH_TOKEN` read, no tokens store, no `/apis/erpbridge.io/v1/tokens` route anywhere in `internal/`). Building SDK auth before the server enforces it is speculative; separate plan per user decision. The `token`/`tokenEnv` config fields stay declared-but-inert so the future plan slots in without an API break.           |
| D4  | **REST surface (v1)**: logs `recent` + `stream` (typed SSE), `metrics` (raw text + parsed), `health`, `cache` (stats + flush), `tools` registry (list/apply/delete), and `invoke` (direct REST tool call).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | User-selected all five areas. Matches the SDK stub feature list (docs/sdk/overview.mdx:20-22). `/api/cache/list` + `/api/cache/inspect` are 501 stubs — excluded.                                                                                                                                                                                                              |
| D5  | **Repo**: new sibling repo `~/Documents/Projects/erpbridge-sdk` (git init, Conventional Commits, tdd workflow per ERPBridge AGENTS.md conventions).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Mirrors the existing project-family layout (ERPBridge / erpbridge-docs are separate repos).                                                                                                                                                                                                                                                                                    |
| D6  | **Tool proxy keys are exact registered names** (e.g. `client.tools.list_employees`), because the server registers bare names. No `erp.` prefix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Evidence: server.go:404 registers bare names; no prefix logic exists. A prefix-normalization option is a possible follow-up, not v1.                                                                                                                                                                                                                                           |
| D7  | **Typed errors**: class hierarchy `ErpbridgeError` → `AuthenticationError` (401), `NotFoundError` (404), `RateLimitError` (429), `ServerError` (5xx), `ProtocolError` (MCP JSON-RPC error). Every REST/MCP failure maps into this tree with the underlying body/message attached.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Gives callers a stable programmatic surface; avoids string-matching on error bodies.                                                                                                                                                                                                                                                                                           |
| D8  | **SSE parsing**: hand-rolled minimal `data: <json>\n\n` line parser (no dependency) in one module.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | The server's SSE format is trivial (docs/api.md:85); the MCP SDK already handles its own SSE. No need for `eventsource` dep.                                                                                                                                                                                                                                                   |
| D9  | **Testing**: TDD with `vitest`; a self-contained `node:http` fixture server that speaks the MCP handshake + the REST endpoints (no nock/msw dep). Optional live integration suite gated by `ERPBridge_TEST_SERVER` env pointing at a real server.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Follows the repo-family TDD rule; the fixture keeps tests hermetic; the gated suite proves real-server behavior.                                                                                                                                                                                                                                                               |
| D10 | **Docs**: SDK docs pages written into the **erpbridge-docs** repo under `docs/sdk/` (already registered); the SDK repo keeps a README quickstart + JSDoc on the public API.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Docs site is the single home for product docs; the SDK stub and sidebar already exist.                                                                                                                                                                                                                                                                                         |
| D11 | **LogRecord typing is lenient**: `{ level: string; msg?: string; time?: string; component?: string; session_id?: string; request_id?: string; tool_name?: string } & Record<string, unknown>`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Server log JSON field names are not formally pinned in docs; strict typing would break on field additions.                                                                                                                                                                                                                                                                     |
| D12 | **Distributed shape (canonical dual, subpaths)**: `"type": "module"` + explicit `exports` map with `types` nested first inside each branch, `.mjs`/`.cjs` + `.d.mts`/`.d.cts`; subpath entries `.` (facade), `./client` (MCP), `./rest` (REST wrappers), `./types`; `./package.json` exported; legacy `main`/`module`/`types` fallbacks; `files: ["dist", "src"]` (ship sources so `declarationMap` go-to-def works); `sideEffects: false`; named exports only, no `export default`.                                                                                                                                                                                                                                                                           | Mirrors the MCP v2 split-package manifest (types-first nested conditions, .d.cts/.d.mts, typesVersions). Dual ESM/CJS still the defensible default for enterprise CJS consumers; `require(esm)` (Node 25.4, stable 2026-01) makes ESM-only viable later but is not v1.                                                                                                         |
| D13 | **Versioning/release**: Conventional Commits + **release-please-action@v4** (release-PR-driven: bot opens a reviewable release PR; merge bumps version + CHANGELOG + tag + GitHub Release); publish via **npm Trusted Publishing (OIDC)** with automatic provenance — no `NODE_AUTH_TOKEN`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Rejected changesets (designed for monorepos; per-PR files are overhead for one package) and semantic-release (fully autonomous, no human gate, and its tag on every merge is noisy). release-please gives the SDK changelog a review step and composes with OIDC publishing (Google Cloud SDK pattern). OIDC is GA since 2025-07-31; provenance is automatic for public repos. |
| D14 | **CI**: Node LTS matrix (`lts/-2`, `lts/-1`, `lts/*`), `npm ci`, jobs for lint / typecheck / test / build; top-level `permissions: contents: read`; all third-party actions **SHA-pinned** with comments; Dependabot for `github-actions` + `npm`; **no caching in release builds**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Least-privilege tokens + SHA pinning are the 2026 hardening baseline (GitHub secure-use docs; action-policy SHA blocking since 2025-08-15).                                                                                                                                                                                                                                    |
| D15 | **Publish gates** (fail the release before it ships): `npm pack --dry-run --json` with file-list assertions, **publint** + **attw --pack** against the packed tarball (tsdown's `lint` option), `npm audit --audit-level=high` + **osv-scanner** (union of two advisory feeds) on PRs and nightly.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Tarball-level validation is the fix for the MCP SDK's own v1.29.0 shipping an exports map pointing at missing files (issue #2273); pnpm and TypeScript's CI both gate on packed-tarball checks.                                                                                                                                                                                |
| D16 | **Docs deploy**: separate `docs.yml` building **TypeDoc** and deploying to GitHub Pages via the official `configure-pages` → `upload-pages-artifact` → `deploy-pages` chain on the `github-pages` environment (`pages: write` + `id-token: write`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Rejected `gh-pages` branch push (peaceiris) and publishing docs in the release workflow (keeps Pages permissions out of the publish job). Product guides still live in erpbridge-docs (D10); TypeDoc covers the SDK's API reference.                                                                                                                                           |
| D17 | **Plan split (user decision)**: the SDK auth effort is a separate plan — `.agents/plans/Plan-auth.md`, marked **Future** — not part of the v1 execution plan. The main plan (this file) is auth-free. Auth work starts only when the server's auth (`Plan-Auth.md`) is implemented and merged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Keeps v1 shippable against today's open server; the future plan carries the bearer resolution, injection, 401 mapping, scope awareness, and `docs/sdk/authentication.mdx`.                                                                                                                                                                                                     |

## Scope

**In (v1, TypeScript):** `createClient(config)` + capability groups (MCP tools, REST wrappers, config); typed errors; `node:http` fixture tests + gated live integration suite; dual ESM+CJS build via tsdown with subpath exports; packed-consumer smoke milestone after assembly; release-please + npm trusted-publishing pipeline; GitHub Actions CI/release/docs workflows with publish gates; README; docs-site `docs/sdk/*` pages; CHANGELOG.

**Out (v1):** Python/Go ports; **auth (token resolution, bearer injection, `ERPBridge_TOKEN`, scope awareness) — deferred to the future `.agents/plans/Plan-auth.md`**; token management (create/revoke); stdio transport in the SDK (agents use server stdio directly); `erp.`-prefix normalization; `/api/cache/list|inspect`; codegen of typed tool signatures from JSON schemas (manual proxy is enough for 16 tools); `docs/sdk/authentication.mdx`.

### Documentation pairing

Every task that adds or changes public behavior updates the relevant README content, `CHANGELOG.md` **Unreleased** entry, and the matching `erpbridge-docs/docs/sdk/` guide in the same change set. T14 consolidates, cross-links, and verifies those accumulated documentation updates; it is not permission to defer feature documentation. Versioned changelog entries and package-version changes remain release-please-owned.

## Tasks

- [x] **T1 — Repo scaffold** — git init; `package.json` (`@erpbridge/sdk`, version `0.1.0`, `type: module`, per-D12 exports map, `engines.node >= 20`, `files: ["dist","src"]`, `sideEffects: false`, MIT license, repository, keywords; deps `@modelcontextprotocol/client@~2.0` (D2); devDeps `typescript`, `tsdown@^0.22`, `vitest`, `publint`, `@arethetypeswrong/cli`, `typedoc`); committed `package-lock.json`; `tsconfig.json` (strict, `module: nodenext`, `moduleResolution: nodenext`, `verbatimModuleSyntax`, `isolatedModules`, `isolatedDeclarations`, `declaration`, `declarationMap`, `target: es2022`); `tsdown.config.ts` (ESM+CJS, dts, external deps, lint publint+attw); `vitest.config.ts`; `src/index.ts` (empty export); `.gitignore`; `CHANGELOG.md` (Unreleased); minimal README.
      (**Seam:** n/a — greenfield; **Files:** package.json, package-lock.json, tsconfig.json, tsdown.config.ts, vitest.config.ts, src/index.ts, .gitignore, CHANGELOG.md, README.md; **Verify:** `npm install && npm ci && npm run build && npm test` all pass)
- [x] **T1a — Package and release configuration** — establish `release-please-config.json`, `.release-please-manifest.json`, `.commitlintrc.json`, and the `npm run lint:publish` script (publint + attw --pack + `npm pack --dry-run --json` file assertions) before any packaged-consumer milestone. This config is version-control metadata only; release-please remains the sole owner of version bumps and versioned changelog entries.
      (**Seam:** package boundary; **Files:** release-please-config.json, .release-please-manifest.json, .commitlintrc.json, package.json; **Verify:** `npm test && npm run build && npm run lint:publish` green against the scaffold tarball)
- [x] **T2 — Types module** — `src/types.ts`: `ErpbridgeConfig` (baseUrl default `http://localhost:8080`, mcpUrl default `${baseUrl}/mcp/`, `token?`/`tokenEnv?` declared-but-inert, reserved for the future auth plan per D17, timeoutMs default 15_000, fetch? override), `LogRecord` (per D11), `ToolDefinition`, `ToolResult { result: unknown; error?: string; isError?: boolean }`, `CacheStats`, `MetricFamily`/`MetricSample`, `ToolCallArguments`, and the error classes from D7.
      (**Seam:** n/a; **Files:** src/types.ts, src/types.test.ts; **Verify:** `npm test` — type assertions + unit tests for error class hierarchy)
- [x] **T3 — Config normalization** — `src/config.ts`: `resolveConfig(input)` normalizes baseUrl/mcpUrl and fills defaults (timeouts, fetch). No token resolution, no auth warning (auth is the future Plan-auth.md, D17).
      (**Seam:** consumed by client factory; **Files:** src/config.ts, src/config.test.ts; **Verify:** `npm test` — default-URL derivation + override precedence)
- [x] **T4 — HTTP core** — `src/http.ts`: fetch wrapper with base URL join, JSON encode/decode, `timeoutMs` abort, and response mapping into the D7 error tree (401→`AuthenticationError` with `WWW-Authenticate` hint, 404→`NotFoundError`, 429→`RateLimitError`, 5xx→`ServerError`). Injectable `fetch` for tests. No Authorization handling in v1 (auth deferred, D17).
      (**Seam:** single seam for all REST calls; **Files:** src/http.ts, src/http.test.ts; **Verify:** `npm test` — fixture server returns each status; asserts error mapping)
- [x] **T5 — MCP client wrapper** — `src/mcp.ts`: wraps `Client` + `StreamableHTTPClientTransport` from `@modelcontextprotocol/client@~2.0.0` (D2). **Step 1 — compatibility spike (Rec1):** a minimal `initialize` + `tools/list` round-trip against a real ERPBridge server at `ERPBridge_TEST_SERVER`, confirming version negotiation (client `2025-11-25` ↔ mcp-go v0.57) and that the v2 client's OAuth protected-resource metadata probe (`/.well-known/oauth-protected-resource`) falls back gracefully — the server hosts no such metadata. If negotiation fails, stop and revisit the pin (see Open Questions). Then: `connect()`, `listTools()` → `ToolDefinition[]`, `callTool(name, args)` → `ToolResult` (map MCP content items to `result`/`isError`). A server-recognized tool execution failure returns `ToolResult { isError: true }`; the fixture's defined MCP “unknown tool” protocol error maps to `NotFoundError`. Reconnect = **one retry that creates a new transport + full re-initialize** (new session ID), then throw `ProtocolError` (R2).
      (**Seam:** official client transport; **Files:** src/mcp.ts, src/mcp.test.ts, fixtures/mcp-server.ts, package.json; **Verify:** add and run `ERPBridge_TEST_SERVER=<server-url> npm run test:mcp-compat` for the spike first; `npm test` — fixture completes initialize/listTools/callTool, distinguishes execution errors from unknown-tool `NotFoundError`, and reconnect re-initializes once then throws; `npm run build`)
- [ ] **T6 — Typed tool proxy** — `src/tools.ts`: a `Proxy`-based `client.tools.<exactName>(args)` that lazily discovers tools on first call, validates arguments against the discovered `inputSchema` (draft-07 subset: required + type), and delegates to `callTool`. Unknown names throw `NotFoundError` with the available tool list in the message.
      (**Seam:** sits on T5; **Files:** src/tools.ts, src/tools.test.ts; **Verify:** `npm test` — proxy call for `list_employees`, unknown-name error, schema-mismatch error)
- [ ] **T7 — Logs REST** — `src/logs.ts`: `recent()` → `LogRecord[]` (GET `/api/logs/recent`), `stream(opts?)` → `AsyncIterable<LogRecord>` parsing `data: <json>\n\n` SSE (D8), auto-reconnecting until an `AbortController`/`opts.signal` is fired.
      (**Seam:** T4 http core; **Files:** src/logs.ts, src/logs.test.ts, fixtures/logs-sse.ts; **Verify:** `npm test` — recent() array shape; stream() emits records from fixture SSE and aborts cleanly)
- [ ] **T8 — Metrics REST** — `src/metrics.ts`: `text()` → raw Prometheus text body; `parsed()` → `{ families: MetricFamily[] }` parsing **counters, gauges, and histograms only** (`_bucket`/`_sum`/`_count`) — the types the server actually emits (`erp_requests_total`, `mcp_tool_invocations_total`, `cache_hits_total`, `mcp_sessions_active`, `erp_request_duration_seconds`, `mcp_tool_duration_seconds`; no summaries). Parser boundary documented in `src/prometheus.ts` (line-oriented, label sets, float values); a dependency like `prom-client-parser` is only considered if the server adds summary metric types (R4). **`fixtures/metrics.txt` must be a real `/metrics` snapshot grabbed from the server (`curl localhost:8080/metrics`), not hand-written** — ensures label names and histogram buckets match reality.
      (**Seam:** T4 http core; **Files:** src/prometheus.ts, src/metrics.ts, src/metrics.test.ts, fixtures/metrics.txt; **Verify:** `npm test` — parses fixture text into expected families/samples incl. histograms; rejects/skips summary families with a clear error)
- [ ] **T9 — Health + cache** — `src/system.ts`: `health()` → `{ status: string }` (GET `/mcp/health`); `cache.stats()` → `CacheStats` (GET `/api/cache/stats`, maps 503 → typed `ServerError` with message "cache not enabled"); `cache.flush({tool?, module?, all?})` → `{ deleted, status }` (GET `/api/cache/flush`).
      (**Seam:** T4 http core; **Files:** src/system.ts, src/system.test.ts; **Verify:** `npm test` — fixture returns documented shapes; 503 maps correctly)
- [ ] **T10 — Tool registry + direct invoke** — `src/registry.ts`: `registry.list()` (GET `/apis/erpbridge.io/v1/tools`), `registry.apply(def)` (POST, expects 201, maps 400 admission errors), `registry.delete(name, version, {hard?})` (DELETE); `invoke(name, args)` → `ToolResult` (POST `/api/tools/invoke`, maps 404/500). **Note (C3):** `client.registry.list()` (REST registry) and `client.mcp.listTools()` (MCP protocol) are distinct surfaces returning different shapes. Registry CRUD deliberately lives outside `client.tools` so exact registered proxy names such as `list`, `apply`, or `delete` are never shadowed; document both in T11b.
      (**Seam:** T4 http core; **Files:** src/registry.ts, src/registry.test.ts; **Verify:** `npm test` — full CRUD + invoke against fixture; admission-error mapping; `npm run build`)
- [ ] **T11a — Public API assembly** — `src/index.ts` exports `createClient(config): ErpbridgeClient` exposing `.mcp` (connect/listTools/callTool), `.tools` (exact-name MCP proxy only), `.registry` (REST registry CRUD), `.invoke`, `.logs`, `.metrics`, `.health`, `.cache`, `.close()`; subpath entry files `src/client.ts` (`@erpbridge/sdk/client`), `src/rest.ts` (`@erpbridge/sdk/rest`), `src/types.ts` (`@erpbridge/sdk/types`); JSDoc on every public symbol. Add `src/index.test.ts` using the fixture server to construct the public facade and exercise representative MCP and REST capabilities through it. No auth params beyond the inert `token`/`tokenEnv` config fields (D17).
      (**Seam:** n/a — assembly; **Files:** src/index.ts, src/index.test.ts, src/client.ts, src/rest.ts; **Verify:** `npm test && npm run build && npm pack --dry-run` — public facade and exports resolve)
- [ ] **T11b — README** — README with install + 5-line quickstart (`createClient({ baseUrl })` → `await client.tools.list_employees({})`), error-handling section, and the C3 note distinguishing `registry.list()` (registry REST) from `mcp.listTools()` (MCP protocol). No auth section in v1 — link to the future auth plan instead. Pair this README update with the matching `CHANGELOG.md` Unreleased and `erpbridge-docs` updates required by the documentation-pairing rule.
      (**Seam:** n/a — writing; **Files:** README.md, CHANGELOG.md; **Verify:** `npm test && npm run build && npm run lint:publish` green; README renders on npmjs package page preview; `npm run build` green in erpbridge-docs)
- [ ] **T12 — Packed-consumer smoke milestone (R5)** — after T11b and T1a, run `npm run build && npm run lint:publish && npm pack`, install the tarball into a scratch project and smoke-test `createClient` + `mcp.listTools()` + `logs.recent()`; **also import every subpath (`@erpbridge/sdk/client`, `/rest`, `/types`) to verify the exports map resolves in a real consumer** (catches the class of bug the MCP SDK v1.29.0 had). This is an internal package-shape check: do not change the package version, create a versioned CHANGELOG entry, tag, create a GitHub release, or publish to npm.
      (**Seam:** n/a — milestone; **Files:** scratch smoke script only (uncommitted); **Verify:** `npm test && npm run build && npm run lint:publish`; packed tarball installs in scratch project; smoke script runs against fixture and imports all subpaths)
- [ ] **T13 — Live integration suite** — `tests/integration/` gated behind the `ERPBridge_TEST_SERVER` env var (skips otherwise). Three sub-files:

      **`integration.test.ts`** — happy paths:
      1. `health()` → `{ status: "ok" }`
      2. `mcp.connect()` + `mcp.listTools()` → includes the built-in tools (`system.progress_test`, `system.sensitive_log_test`)
      3. `mcp.callTool("system.progress_test", {})` → `ToolResult`
      4. `tools.<tool>(args)` via proxy (requires seeded tools)
      5. `invoke("system.progress_test", {})` → `ToolResult` via REST
      6. `logs.recent()` → non-empty `LogRecord[]` after tool calls
      7. `logs.stream()` → emits ≥1 record within 10s after a tool call, then aborts cleanly
      8. `metrics.text()` → contains `mcp_tool_invocations_total`
      9. `metrics.parsed()` → families include counters + histograms
      10. `cache.stats()` → `CacheStats` (redis) or `ServerError` (no redis)
      11. `registry.list()` (registry REST) → array with seeded tools
      12. `registry.apply()` + `registry.delete()` → registry CRUD round-trip

      **`integration-errors.test.ts`** — error paths:
      1. `mcp.callTool("nonexistent_tool", {})` → `NotFoundError`
      2. `invoke("nonexistent_tool", {})` → mapped error
      3. `cache.flush()` without redis → `ServerError` (503)
      4. real 404/500 where triggerable — verify at T13 time whether `system.sensitive_log_test` actually 500s

      **`tests/integration/README.md`** — runbook: prerequisites (Docker or Go + Python/uv); server startup via Docker Compose (recommended) — `docker compose up -d --build` in the ERPBridge repo, poll `/mcp/health` for readiness, `make generate-tools` to seed tools; health-check polling before the run; full command `ERPBridge_TEST_SERVER=http://localhost:8080 npm run test:integration`; teardown `docker compose down -v`; why integration tests are not in CI yet (future path: Docker Compose service in the CI workflow or a dedicated `integration.yml`).

      (**Seam:** real server via `docker compose up -d --build` in ERPBridge repo; **Files:** tests/integration/integration.test.ts, tests/integration/integration-errors.test.ts, tests/integration/README.md; **Verify:** `ERPBridge_TEST_SERVER=http://localhost:8080 npm run test:integration` green against the Docker Compose stack with seeded tools; reports saved to `.scratch/testing/` per Plan-testing.md)

- [ ] **T14 — Docs-site completeness and release validation** — in **erpbridge-docs** repo (separate repo, `docs/sdk/`): consolidate and cross-link the documentation updates paired with T2–T11b; replace stub `overview.mdx` with a real landing page; add `installation.mdx`, `quickstart.mdx` (Tabs for npm/pnpm), `mcp-tools.mdx`, `logs-metrics.mdx`; update `docs/erpbridge/intro.mdx` components table + `docs/roadmap/overview.mdx` Phase 3 to list the SDK. The agent guide page (`docs/sdk/agent-guide.mdx`) already exists and mirrors this repo's AGENTS.md. `authentication.mdx` is deferred to the future auth plan.
      (**Seam:** docs/sdk/ already in sidebar/navbar (sidebars.ts:80, docusaurus.config.ts:136-141); **Files:** docs/sdk/*.mdx, docs/erpbridge/intro.mdx, docs/roadmap/overview.mdx, CHANGELOG.md Unreleased; **Verify:** `npm test && npm run build && npm run lint:publish` green in this repo; `npm run build` green in erpbridge-docs — fails on broken links/MDX)
- [ ] **T15 — CI workflow** — `.github/workflows/ci.yml`: Node LTS matrix (`lts/-2`, `lts/-1`, `lts/*`), `npm ci`, jobs for typecheck / lint / test / build / publish-gate; top-level `permissions: contents: read`; SHA-pinned actions (checkout + setup-node with comments); `npm audit --audit-level=high` + `google/osv-scanner-action` PR scan; `npm pack --dry-run --json` assertions.
      (**Seam:** n/a — repository automation; **Files:** .github/workflows/ci.yml, .github/dependabot.yml; **Verify:** `npm test && npm run build && npm run lint:publish`; workflow passes on a local push; gates fail on a deliberately broken pack)
- [ ] **T16 — API docs workflow** — `.github/workflows/docs.yml` + `typedoc.json`: build TypeDoc from `src/` into `docs/api/` on push to `main` and release tags; deploy via `actions/configure-pages` → `actions/upload-pages-artifact` → `actions/deploy-pages` with `permissions: pages: write` + `id-token: write` on the `github-pages` environment.
      (**Seam:** n/a — repository automation; **Files:** .github/workflows/docs.yml, typedoc.json; **Verify:** `npm test && npm run build && npm run lint:publish`; `npm run docs` emits `docs/api/`; Pages deploy succeeds after enabling Pages > Source: GitHub Actions)
- [ ] **T17 — npm Trusted Publishing bootstrap** — before the first automated release, an authorized package maintainer creates `@erpbridge/sdk` on npm through a one-time local publish with a narrowly scoped, short-lived granular automation token, configures the repository/workflow as npm's trusted publisher, then revokes the token. Do not place that token in repository files, CI secrets, logs, or test reports. This is an operator-run prerequisite, not a CI workflow step.
      (**Seam:** npm registry; **Files:** none; **Verify:** npm package settings show the trusted publisher; a subsequent CI OIDC dry-run/controlled release is authorized without `NODE_AUTH_TOKEN`)
- [ ] **T18 — Release workflow** — `.github/workflows/release.yml`: job 1 runs `googleapis/release-please-action@v4` (outputs `release_created`); job 2 (needs: release) publishes with npm **trusted publishing (OIDC)** — `permissions: id-token: write`, no `NODE_AUTH_TOKEN`, `npm ci && npm run build && npm run lint:publish && npm publish`, no caching; job 3 creates the GitHub Release notes. **Every release updates erpbridge-docs** (`docs/sdk/` pages + agent-guide sync + CHANGELOG Unreleased) — open/merge that docs PR in the same release cycle. Requires T16 and T17, a public repo, and a `repository.url` matching the GitHub repo (provenance requirement).
      (**Seam:** n/a — repository automation; **Files:** .github/workflows/release.yml; **Verify:** release PR opens on a `feat:` commit; after T17, merge publishes `0.1.0` with provenance; docs PR merged in the same cycle; Pages workflow green; `npm audit`/osv clean)

## Verification

1. Every implementation task follows the TDD and per-task execution sequence in `Plan-testing.md`: focused expected-red/green test, task-specific verification, then `npm test && npm run build`; package artifacts additionally run `npm run lint:publish`.
2. `npm run build` produces valid ESM + CJS `dist/`; `npm pack --dry-run` contains `dist/`, `src/`, README, package.json — and nothing else.
3. **Package shape**: `npm run lint:publish` (publint + attw --pack) green against the tarball — exports resolve under `node10`/`node16`/`bundler`; `require` branch resolves to CJS with `.d.cts`; subpaths `./client`, `./rest`, `./types`, `./package.json` resolve.
4. **MCP**: T5 live compatibility spike green first (initialize + tools/list against the real server, v2 client ↔ mcp-go 2025-11-25); `createClient({ baseUrl }).mcp.listTools()` returns the 14 schemas' names; `client.tools.list_employees({})` executes; fixture-defined MCP unknown tool throws `NotFoundError`; recognized tool failures return `ToolResult { isError: true }`; reconnect re-initializes once then throws `ProtocolError`.
5. **REST**: `logs.recent()`, `logs.stream()` (SSE), `metrics.parsed()` (counters + gauges + histograms, summaries rejected), `health()`, `cache.stats()`, `registry.apply/list/delete`, `invoke()` all round-trip against the fixture and (gated) the live server.
6. **Packed consumer**: the current workspace tarball installs into a scratch project, imports all public subpaths, and its smoke script runs against the fixture without a manual version/changelog/tag/release change.
7. **CI**: workflow green on the Node LTS matrix; publish-gate job fails on a broken tarball; audit + osv scans clean.
8. **Release**: after the documented trusted-publishing bootstrap, a `feat:` commit on `main` opens a release PR; merge publishes `0.1.0` to npm with provenance; tag + GitHub Release created; the already-configured Pages workflow deploys; docs-site PR is merged in the same release cycle.
9. **Docs**: erpbridge-docs `npm run build` green with the new SDK pages; llms.txt artifacts regenerate.
10. **Auth deferred**: v1 ships with no token handling; server 401s surface as `AuthenticationError`. Auth behavior is verified under the future `Plan-auth.md`, not here.
11. **Test reporting**: every meaningful test run saves a summarized agent-readable report to `.scratch/testing/` per `Plan-testing.md`; unexpected failures and flakes are always saved and trigger a testing-plan amendment before the next task starts.

## Open Questions

- **npm publishing bootstrap**: trusted publishing (OIDC) requires the package to already exist on npmjs.com. T17 is the explicit operator-run bootstrap prerequisite; do not bypass it or retain its temporary granular token after configuration.
- **MCP version alignment (R1, settled)**: `@modelcontextprotocol/client@~2.0.0` is pinned because its protocol set (`2025-11-25` latest, negotiable to `2025-03-26`) matches the server's mcp-go v0.57.0. The only residual risk is the v2 client's OAuth metadata probe on the server that hosts no `/.well-known/oauth-protected-resource` — verified during the T5 spike. Fallback to `@modelcontextprotocol/sdk@^1.30.0` only if the spike fails.
- **Server MCP upgrade (tracking)**: ERPBridge's `Plan-MCP-Upgrade.md` (future) migrates to mcp-go v1.0.0-beta.1 / spec `2026-07-28`. When that lands, re-evaluate the SDK's client pin (D2). Not blocking v1.
- **Server auth (tracking)**: ERPBridge's `Plan-Auth.md` (A1–A10) is unimplemented. SDK auth starts when it ships — see `.agents/plans/Plan-auth.md` (future).
- **Prefix normalization**: server registers bare tool names today; if the server later adds the `erp.` prefix (AGENTS.md says it), the proxy key strategy in D6 may need a normalization option. Tracked, not blocking v1.
