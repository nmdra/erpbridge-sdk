# Plan: ERPBridge SDK (TypeScript) — v0.1.0

## Goal

Ship a **TypeScript client library** (`@erpbridge/sdk`) that gives application and AI-agent developers a single, ergonomic way to consume an ERPBridge server — MCP tool calls, REST tool invocation, log aggregation, metrics, cache, and health — **without hand-rolling JSON-RPC, SSE parsing, or retry logic**. Authentication support ships in a separate future plan (see D17) once the server's own auth lands.

For whom: developers embedding ERPBridge into agentic-AI workflows, web backends, and monitoring tooling (the audience the docs site already targets in `docs/sdk/overview.mdx`).

Why: today the only options are bridgectl (a CLI) or copy-pasting the hand-rolled client examples in `docs/erpbridge/mcp-client-guide.mdx`. Neither provides a library that handles typed results, session reconnects, and typed REST wrappers.

Decisions confirmed with user: **TypeScript** (v1), **wrap the official MCP client** (`@modelcontextprotocol/client@~2.0.0`, aligned to the server's mcp-go spec), **auth deferred to a future plan**, REST surface = **logs + metrics + health + cache + tool registry + direct invoke**, repo at **`~/Documents/Projects/erpbridge-sdk`**.

## Current State

Evidence gathered from the three project repos (server, docs, prototype):

- **Server** (`~/Documents/Projects/ERPBridge`, Go, mcp-go v0.57.0):
  - MCP streamable HTTP at `/mcp/` (stateful, session idle TTL 30 min) + stdio; `initialize`/`tools/list`/`tools/call` lifecycle (`internal/mcp/server.go:465-551`). Tools are registered **bare-named** (`list_employees`), NOT `erp.`-prefixed; the `erp.` prefix in AGENTS.md is not implemented (`internal/mcp/server.go:404`).
  - REST surface (docs/api.md): `POST /api/tools/invoke` `{name, arguments}` → `{result, error?, isError?}` (500 on tool error); `/apis/erpbridge.io/v1/tools` GET/POST/DELETE (registry CRUD); `/api/logs/recent` (JSON array, last 1000) and `/api/logs/stream` (SSE `data: <json>\n\n`); `/api/cache/stats` + `/api/cache/flush?tool&module&all`; `/mcp/health` → `{"status":"ok"}`; `/metrics` (Prometheus text).
  - **Auth is planned, not shipped**: `.agents/plans/Plan.md` A1–A10 all unchecked. Intended design (docs/erpbridge/auth.mdx:62-74): `API_AUTH_TOKEN` admin env var; `erpbt_`-prefixed API tokens, SHA-256-hashed in SQLite; scopes `mcp|metrics|logs`; CRUD at `/apis/erpbridge.io/v1/tokens`; bearer enforcement only when `API_AUTH_TOKEN` is set (open-mode back-compat). CORS on `/mcp/` **already allows** the `Authorization` header (`internal/mcp/server.go:472`), so the SDK can send tokens today.
  - Metric names (internal/metrics/metrics.go:8-69): `erp_requests_total{method,path,status}`, `erp_request_duration_seconds{method,path}`, `mcp_tool_invocations_total{tool,cache_status}`, `mcp_tool_duration_seconds{tool}`, `cache_hits_total{type}`, `cache_misses_total`, `mcp_sessions_active`, `mcp_server_starts_total`, `mcp_server_stops_total`, `mcp_sessions_started_total`, `mcp_sessions_ended_total`.
  - Log record fields (internal/logger + middleware.go:83-91): `level`, `msg`, `time`, `component`, `session_id`, `request_id`, `tool_name` (JSON, redacted).
- **Docs site** (`~/Documents/Projects/erpbridge-docs`, Docusaurus 3 / TS, Node >= 20): `docs/sdk/` section, `sdkSidebar`, navbar "SDK" item, and footer link **already registered** (`docs/sdk/overview.mdx` is a WIP stub listing exactly: built-in MCP client, log aggregation, view metrics). New pages dropped into `docs/sdk/` appear automatically (autogenerated sidebar, sidebars.ts:80). Adding SDK docs needs **no config changes**.
- **Prototype** (`~/Documents/Projects/erpbridge-client-test/erpbridge-client`): a scratch demo script (official Python `mcp` SDK + ollama chat loop). Not a library; no auth, no REST wrapping. Informative only — not reused.

### External research (TS SDK practices, 2025–2026, verified 2026-08-20)

- **Build tooling**: tsdown (Rolldown-based) is the fast-growing successor to tsup and is what the official MCP TypeScript SDK v2 builds with (`format: ['esm','cjs']` + `fixedExtension`, PR #2405); tsup remains the battle-tested fallback with a compatible API in v0.22.x (PkgPulse comparison, pkgpulse.com/guides/tsup-vs-tsdown-vs-unbuild-typescript-library-bundling-2026). tsdown auto-generates `exports` and integrates publint/attw validation (tsdown.dev/guide).
- **MCP SDK split (v2)**: `@modelcontextprotocol/sdk` v1 is monolithic — the client drags in server deps (express, hono, cors, npm registry). The v2 line splits into `@modelcontextprotocol/client`, `@modelcontextprotocol/server`, `@modelcontextprotocol/core`; `client@2.0.0` is client-only, Node >= 20, ESM-first + CJS, deps: zod, jose, cross-spawn, eventsource, eventsource-parser, pkce-challenge (npm registry, checked 2026-08-20).
- **Dual ESM/CJS**: canonical 2026 manifest = `"type": "module"` + `exports` with `types` nested first in each `import`/`require` branch, `.mjs`/`.cjs` + `.d.mts`/`.d.cts`, legacy `main`/`module`/`types` fallbacks (Node docs nodejs.org/api/packages.html; MCP client package.json as the working example). One shared `.d.ts` for both branches leaves CJS consumers wrong-typed under `node16` (javascript-package-publishing.com). `require(esm)` stable in Node 25.4.0 (2026-01) makes ESM-only defensible later, but dual remains the enterprise-safe default (SnipShift 2026-07).
- **Types/config**: author for `module/moduleResolution: nodenext` (the only correct module options for Node libs — typescriptlang.org modules reference); `verbatimModuleSyntax` + `isolatedModules` (+ `isolatedDeclarations` TS 5.5+) are the recommended library flags; run `tsc --noEmit` as a separate typecheck gate (js-package-publishing).
- **Source maps**: ship `declarationMap` **only if `src/` ships too** (go-to-def lands on missing files otherwise); plain `.js.map` is bloat for unminified output (web-standards.dev 2026-08).
- **Metadata**: `files` allowlist (`["dist","src"]`), `sideEffects: false` only when truly side-effect-free, SPDX `license`, `engines` advisory, `./package.json` exported for tooling (npm docs; webpack tree-shaking guide).
- **Subpath exports** are the SDK norm (MCP v2 ships `.`/`./stdio`/`./validators/*` with a runtime-neutral root — ts.sdk.modelcontextprotocol.io/v2/get-started/packages.html). Cautionary tale: MCP SDK v1.29.0's exports map pointed at files missing from the tarball, breaking root import (issue #2273) — tarball-level validation is mandatory.
- **Versioning/release**: for a standalone package with 1–2 maintainers, **release-please** (PR-driven, human gate, composes with npm publish) beats changesets (monorepo-first by design — changesets/docs/decisions.md) and semantic-release (no review step, tag on every merge) (latchkey.dev, tenki.cloud 2026 comparisons).
- **npm publishing**: **Trusted Publishing (OIDC) is GA since 2025-07-31** — no `NODE_AUTH_TOKEN`, provenance automatic for public repos; requires npm >= 11.5.1, Node >= 22.14, GitHub-hosted runners, and the package to already exist on npmjs.com; `repository.url` must exactly match the GitHub repo (github.blog changelog 2025-07-31; docs.npmjs.com/trusted-publishers/).
- **CI shape**: Node LTS matrix (`lts/-2`…`lts/*`), `npm ci`, separate lint/typecheck/test/build jobs; top-level `permissions: contents: read`; SHA-pin all third-party actions (GitHub's allowed-actions policy can block unpinned actions since 2025-08-15); Dependabot for `github-actions` + `npm`; no caching in release builds (docs.github.com secure-use; npm trusted-publishers example).
- **Publish gates**: `npm pack --dry-run --json` file assertions + **publint** + **attw --pack** (they catch different failure classes — publint.dev/docs/comparisons) + `npm audit --audit-level=high` AND **osv-scanner** (different advisory feeds; union is the finding set) — on PRs and a scheduled nightly scan (google/osv-scanner-action).
- **Docs deploy**: official chain `actions/configure-pages` → `upload-pages-artifact` → `deploy-pages` on the `github-pages` environment with `pages: write` + `id-token: write`; separate workflow from release; not a `gh-pages` branch push (docs.github.com Pages custom workflows; k0swe PR #820).

## Decisions

| #   | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Rationale / rejected                                                                                                                                                                                                                                                                                                                                                           |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D1  | **Language & packaging**: TypeScript, strict mode, ESM + CJS dual build via **tsdown** (Rolldown-based, the tool the MCP TypeScript SDK itself builds with), Node >= 20, package name `@erpbridge/sdk`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Matches docs site (TS); agentic-AI ecosystem is JS/TS-heavy. Python/Go deferred to a follow-up release (user-confirmed). tsdown chosen over tsup (battle-tested fallback, tsup-compatible API in v0.22.x) for Rolldown momentum, auto-`exports` generation, and built-in publint/attw validation (tsdown.dev/guide; MCP SDK builds with tsdown, PR #2405).                     |
| D2  | **MCP core**: wrap official `@modelcontextprotocol/client@~2.0.0` (client-only split package) `Client` + `StreamableHTTPClientTransport`. **Version aligned to the server's spec**: the server runs mcp-go v0.57.0 with `LATEST_PROTOCOL_VERSION = "2025-11-25"` and negotiation down to `2025-03-26`; `@modelcontextprotocol/core@2.0.0` exposes the identical set (`LATEST = "2025-11-25"`, `DEFAULT_NEGOTIATED = "2025-03-26"`, supported `[2025-11-25, 2025-06-18, 2025-03-26, 2024-11-05, 2024-10-07]` — verified in the published bundle). `~2.0.0` pins the 2.0.x surface for v0.1.0 stability; Dependabot PRs surface 2.1+ for human review. Re-evaluate when the server's future `Plan-MCP-Upgrade.md` (mcp-go v1.0.0-beta.1, spec 2026-07-28) lands. | Rejected hand-rolled JSON-RPC (docs guide's style is pedagogical only) and rejected v1 `@modelcontextprotocol/sdk`: it pulls server deps (express, hono, cors) we never use; the v2 `@modelcontextprotocol/client` is client-only, Node >= 20, ESM-first + CJS (npm registry, checked 2026-08-20).                                                                             |
| D3  | **Auth is deferred — v1 ships unauthenticated**: no token resolution, no bearer injection, no `ERPBridge_TOKEN` handling, no no-token warning in v1. The SDK connects anonymously and surfaces server 401s as-is. Auth (bearer resolution/injection, 401 mapping, scope awareness) is a separate **future plan** (`Plan-auth.md`), enabled only once the server's own auth (`ERPBridge/.agents/plans/Plan-Auth.md` A1–A10, currently all unchecked) ships.                                                                                                                                                                                                                                                                                                     | Server auth is unimplemented (verified: no `API_AUTH_TOKEN` read, no tokens store, no `/apis/erpbridge.io/v1/tokens` route anywhere in `internal/`). Building SDK auth before the server enforces it is speculative; separate plan per user decision. The `token`/`tokenEnv` config fields stay declared-but-inert so the future plan slots in without an API break.           |
| D4  | **REST surface (v1)**: logs `recent` + `stream` (typed SSE), `metrics` (raw text + parsed), `health`, `cache` (stats + flush), `tools` registry (list/apply/delete), and `invoke` (direct REST tool call).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | User-selected all five areas. Matches the SDK stub feature list (docs/sdk/overview.mdx:20-22). `/api/cache/list` + `/api/cache/inspect` are 501 stubs — excluded.                                                                                                                                                                                                              |
| D5  | **Repo**: new sibling repo `~/Documents/Projects/erpbridge-sdk` (git init, Conventional Commits, tdd workflow per ERPBridge AGENTS.md conventions).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Mirrors the existing project-family layout (ERPBridge / erpbridge-docs are separate repos).                                                                                                                                                                                                                                                                                    |
| D6  | **Tool proxy keys are exact registered names** (e.g. `client.tools.list_employees`), because the server registers bare names. No `erp.` prefix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Evidence: server.go:404 registers bare names; no prefix logic exists. A prefix-normalization option is a possible follow-up, not v1.                                                                                                                                                                                                                                           |
| D7  | **Typed errors**: class hierarchy `ErpbridgeError` → `AuthenticationError` (401), `NotFoundError` (404), `RateLimitError` (429), `ClientError` (4xx — other than 401/404/429; RF3), `ServerError` (5xx), `ProtocolError` (MCP JSON-RPC error). Every REST/MCP failure maps into this tree with the underlying body/message attached.                                                                                                                                                                                                                                                                                                                                                                                                     | Gives callers a stable programmatic surface; avoids string-matching on error bodies. `ClientError` added in RF3 to honor D7's "ServerError (5xx)" definition — 4xx (422 admission, 400) now map correctly. |
| D8  | **SSE parsing**: hand-rolled minimal `data: <json>\n\n` line parser (no dependency) in one module.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | The server's SSE format is trivial (docs/api.md:85); the MCP SDK already handles its own SSE. No need for `eventsource` dep.                                                                                                                                                                                                                                                   |
| D9  | **Testing**: TDD with `vitest`; a self-contained `node:http` fixture server that speaks the MCP handshake + the REST endpoints (no nock/msw dep). Optional live integration suite gated by `ERPBridge_TEST_SERVER` env pointing at a real server.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Follows the repo-family TDD rule; the fixture keeps tests hermetic; the gated suite proves real-server behavior.                                                                                                                                                                                                                                                               |
| D10 | **Docs**: SDK docs pages written into the **erpbridge-docs** repo under `docs/sdk/` (already registered); the SDK repo keeps a README quickstart + JSDoc on the public API.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Docs site is the single home for product docs; the SDK stub and sidebar already exist.                                                                                                                                                                                                                                                                                         |
| D11 | **LogRecord typing is lenient**: `{ level: string; msg?: string; time?: string; component?: string; session_id?: string; request_id?: string; tool_name?: string } & Record<string, unknown>`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Server log JSON field names are not formally pinned in docs; strict typing would break on field additions.                                                                                                                                                                                                                                                                     |
| D12 | **Distributed shape (canonical dual, subpaths)**: `"type": "module"` + explicit `exports` map with `types` nested first inside each branch, `.mjs`/`.cjs` + `.d.mts`/`.d.cts`; subpath entries `.` (facade), `./client` (MCP), `./rest` (REST wrappers), `./types`; `./package.json` exported; legacy `main`/`module`/`types` fallbacks; `files: ["dist", "src"]` (ship sources so `declarationMap` go-to-def works); `sideEffects: false`; named exports only, no `export default`.                                                                                                                                                                                                                                                                           | Mirrors the MCP v2 split-package manifest (types-first nested conditions, .d.cts/.d.mts, typesVersions). Dual ESM/CJS still the defensible default for enterprise CJS consumers; `require(esm)` (Node 25.4, stable 2026-01) makes ESM-only viable later but is not v1.                                                                                                         |
| D13 | **Versioning/release**: Conventional Commits + **release-please-action@v4** (release-PR-driven: bot opens a reviewable release PR; merge bumps version + CHANGELOG + tag + GitHub Release); publish via **npm Trusted Publishing (OIDC)** with automatic provenance — no `NODE_AUTH_TOKEN`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Rejected changesets (designed for monorepos; per-PR files are overhead for one package) and semantic-release (fully autonomous, no human gate, and its tag on every merge is noisy). release-please gives the SDK changelog a review step and composes with OIDC publishing (Google Cloud SDK pattern). OIDC is GA since 2025-07-31; provenance is automatic for public repos. |
| D14 | **CI**: Node LTS matrix (`lts/-2`, `lts/-1`, `lts/*`), `npm ci`, jobs for lint / typecheck / test / build; top-level `permissions: contents: read`; all third-party actions **SHA-pinned** with comments; Dependabot for `github-actions` + `npm`; **no caching in release builds**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Least-privilege tokens + SHA pinning are the 2026 hardening baseline (GitHub secure-use docs; action-policy SHA blocking since 2025-08-15).                                                                                                                                                                                                                                    |
| D15 | **Publish gates** (fail the release before it ships): `npm pack --dry-run --json` with file-list assertions, **publint** + **attw --pack** against the packed tarball (tsdown's `lint` option), `npm audit --audit-level=high` + **osv-scanner** (union of two advisory feeds) on PRs and nightly (`schedule: cron '0 3 * * *'` + `osv-scanner-reusable.yml`; RF4).                                                                                                                                                                                                                                                                                                                                                                                                     | Tarball-level validation is the fix for the MCP SDK's own v1.29.0 shipping an exports map pointing at missing files (issue #2273); pnpm and TypeScript's CI both gate on packed-tarball checks.                                                                                                                                                                                |
| D16 | **Docs deploy**: separate `docs.yml` building **TypeDoc** and deploying to GitHub Pages via the official `configure-pages` → `upload-pages-artifact` → `deploy-pages` chain on the `github-pages` environment (`pages: write` + `id-token: write`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Rejected `gh-pages` branch push (peaceiris) and publishing docs in the release workflow (keeps Pages permissions out of the publish job). Product guides still live in erpbridge-docs (D10); TypeDoc covers the SDK's API reference.                                                                                                                                           |
| D17 | **Plan split (user decision)**: the SDK auth effort is a separate plan — `.agents/plans/Plan-auth.md`, marked **Future** — not part of the v1 execution plan. The main plan (this file) is auth-free. Auth work starts only when the server's auth (`Plan-Auth.md`) is implemented and merged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Keeps v1 shippable against today's open server; the future plan carries the bearer resolution, injection, 401 mapping, scope awareness, and `docs/sdk/authentication.mdx`.                                                                                                                                                                                                     |

## Scope

**In (v1, TypeScript):** `createClient(config)` + capability groups (MCP tools, REST wrappers, config); typed errors; `node:http` fixture tests + gated live integration suite; dual ESM+CJS build via tsdown with subpath exports; packed-consumer smoke milestone after assembly; release-please + npm trusted-publishing pipeline; GitHub Actions CI/release/docs workflows with publish gates; README; docs-site `docs/sdk/*` pages; CHANGELOG.

**Out (v1):** Python/Go ports; **auth (token resolution, bearer injection, `ERPBridge_TOKEN`, scope awareness) — deferred to the future `.agents/plans/Plan-auth.md`**; token management (create/revoke); stdio transport in the SDK (agents use server stdio directly); `erp.`-prefix normalization; `/api/cache/list|inspect`; codegen of typed tool signatures from JSON schemas (manual proxy is enough for 16 tools); `docs/sdk/authentication.mdx`.

### Documentation pairing

Every task that adds or changes public behavior updates the relevant README content, `CHANGELOG.md` **Unreleased** entry, and the matching `erpbridge-docs/docs/sdk/` guide in the same change set. T14 consolidates, cross-links, and verifies those accumulated documentation updates; it is not permission to defer feature documentation. Versioned changelog entries and package-version changes remain release-please-owned.

## Tasks

- [x] **T1 — Repo scaffold** — git init; `package.json` (`@erpbridge/sdk`, version `0.1.0`, `type: module`, per-D12 exports map, `engines.node >= 20`, `files: ["dist","src"]`, `sideEffects: false`, MIT license, repository, keywords; deps `@modelcontextprotocol/client@~2.0` (D2); devDeps `typescript`, `tsdown@^0.22`, `vitest`, `publint`, `@arethetypeswrong/cli`, `typedoc`); committed `package-lock.json`; `tsconfig.json` (strict, `module: nodenext`, `moduleResolution: nodenext`, `verbatimModuleSyntax`, `isolatedModules`, `isolatedDeclarations`, `declaration`, `declarationMap`, `target: es2022`); `tsdown.config.ts` (ESM+CJS, dts, external deps, lint publint+attw); `vitest.config.ts`; `src/index.ts` (empty export); `.gitignore`; `CHANGELOG.md` (Unreleased); minimal README.
      (**Seam:** n/a — greenfield; **Files:** package.json, package-lock.json, tsconfig.json, tsdown.config.ts, vitest.config.ts, src/index.ts, .gitignore, CHANGELOG.md, README.md; **Verify:** `npm install && npm ci && npm run build && npm test` all pass)
- [x] **T1a — Package and release configuration** — establish `release-please-config.json`, `.release-please-manifest.json`, `.commitlintrc.json`, and the `npm run lint:publish` script (publint + attw --pack + `npm pack --dry-run --json` file assertions) before any packaged-consumer milestone. This config is version-control metadata only; release-please remains the sole owner of version bumps and versioned changelog entries.
      (**Seam:** package boundary; **Files:** release-please-config.json, .release-please-manifest.json, .commitlintrc.json, package.json; **Verify:** `npm test && npm run build && npm run lint:publish` green against the scaffold tarball)
- [x] **T2 — Types module** — `src/types.ts`: `ErpbridgeConfig` (baseUrl default `http://localhost:8080`, mcpUrl default `${baseUrl}/mcp/`, `token?`/`tokenEnv?` declared-but-inert, reserved for the future auth plan per D17, timeoutMs default 15_000, fetch? override), `LogRecord` (per D11), `ToolDefinition`, `ToolResult { result: unknown; error?: string; isError?: boolean }`, `CacheStats`, `MetricFamily`/`MetricSample`, `ToolCallArguments`, and the error classes from D7.
      (**Seam:** n/a; **Files:** src/types.ts, src/types.test.ts; **Verify:** `npm test` — type assertions + unit tests for error class hierarchy)
- [x] **T3 — Config normalization** — `src/config.ts`: `resolveConfig(input)` normalizes baseUrl/mcpUrl and fills defaults (timeouts, fetch). No token resolution, no auth warning (auth is the future Plan-auth.md, D17).
      (**Seam:** consumed by client factory; **Files:** src/config.ts, src/config.test.ts; **Verify:** `npm test` — default-URL derivation + override precedence)
- [x] **T4 — HTTP core** — `src/http.ts`: fetch wrapper with base URL join, JSON encode/decode, `timeoutMs` abort, and response mapping into the D7 error tree (401→`AuthenticationError` with `WWW-Authenticate` hint, 404→`NotFoundError`, 429→`RateLimitError`, 5xx→`ServerError`). Injectable `fetch` for tests. No Authorization handling in v1 (auth deferred, D17).
      (**Seam:** single seam for all REST calls; **Files:** src/http.ts, src/http.test.ts; **Verify:** `npm test` — fixture server returns each status; asserts error mapping)
- [x] **T5 — MCP client wrapper** — `src/mcp.ts`: wraps `Client` + `StreamableHTTPClientTransport` from `@modelcontextprotocol/client@~2.0.0` (D2). **Step 1 — compatibility spike (Rec1):** a minimal `initialize` + `tools/list` round-trip against a real ERPBridge server at `ERPBridge_TEST_SERVER`, confirming version negotiation (client `2025-11-25` ↔ mcp-go v0.57) and that the v2 client's OAuth protected-resource metadata probe (`/.well-known/oauth-protected-resource`) falls back gracefully — the server hosts no such metadata. If negotiation fails, stop and revisit the pin (see Open Questions). Then: `connect()`, `listTools()` → `ToolDefinition[]`, `callTool(name, args)` → `ToolResult` (map MCP content items to `result`/`isError`). A server-recognized tool execution failure returns `ToolResult { isError: true }`; the fixture's defined MCP “unknown tool” protocol error maps to `NotFoundError`. Reconnect = **one retry that creates a new transport + full re-initialize** (new session ID), then throw `ProtocolError` (R2).
      (**Seam:** official client transport; **Files:** src/mcp.ts, src/mcp.test.ts, fixtures/mcp-server.ts, package.json; **Verify:** add and run `ERPBridge_TEST_SERVER=<server-url> npm run test:mcp-compat` for the spike first; `npm test` — fixture completes initialize/listTools/callTool, distinguishes execution errors from unknown-tool `NotFoundError`, and reconnect re-initializes once then throws; `npm run build`)
- [x] **T6 — Typed tool proxy** — `src/tools.ts`: a `Proxy`-based `client.tools.<exactName>(args)` that lazily discovers tools on first call, validates arguments against the discovered `inputSchema` (draft-07 subset: required + type), and delegates to `callTool`. Unknown names throw `NotFoundError` with the available tool list in the message.
      (**Seam:** sits on T5; **Files:** src/tools.ts, src/tools.test.ts; **Verify:** `npm test` — proxy call for `list_employees`, unknown-name error, schema-mismatch error)
- [x] **T7 — Logs REST** — `src/logs.ts`: `recent()` → `LogRecord[]` (GET `/api/logs/recent`), `stream(opts?)` → `AsyncIterable<LogRecord>` parsing `data: <json>\n\n` SSE (D8), auto-reconnecting until an `AbortController`/`opts.signal` is fired.
      (**Seam:** T4 http core; **Files:** src/logs.ts, src/logs.test.ts, fixtures/logs-sse.ts; **Verify:** `npm test` — recent() array shape; stream() emits records from fixture SSE and aborts cleanly)
- [x] **T8 — Metrics REST** — `src/metrics.ts`: `text()` → raw Prometheus text body; `parsed()` → `{ families: MetricFamily[] }` parsing **counters, gauges, and histograms only** (`_bucket`/`_sum`/`_count`) — the types the server actually emits (`erp_requests_total`, `mcp_tool_invocations_total`, `cache_hits_total`, `mcp_sessions_active`, `erp_request_duration_seconds`, `mcp_tool_duration_seconds`; no summaries). Parser boundary documented in `src/prometheus.ts` (line-oriented, label sets, float values); a dependency like `prom-client-parser` is only considered if the server adds summary metric types (R4). **`fixtures/metrics.txt` must be a real `/metrics` snapshot grabbed from the server (`curl localhost:8080/metrics`), not hand-written** — ensures label names and histogram buckets match reality.
      (**Seam:** T4 http core; **Files:** src/prometheus.ts, src/metrics.ts, src/metrics.test.ts, fixtures/metrics.txt; **Verify:** `npm test` — parses fixture text into expected families/samples incl. histograms; rejects/skips summary families with a clear error _(RF10: implemented as skip-and-report via `ParsedMetrics.skipped` — a single summary family never rejects the whole parse; throwing would break the consumer.)_)
- [x] **T9 — Health + cache** — `src/system.ts`: `health()` → `{ status: string }` (GET `/mcp/health`); `cache.stats()` → `CacheStats` (GET `/api/cache/stats`, maps 503 → typed `ServerError` with message "cache not enabled"); `cache.flush({tool?, module?, all?})` → `{ deleted, status }` (GET `/api/cache/flush`).
      (**Seam:** T4 http core; **Files:** src/system.ts, src/system.test.ts; **Verify:** `npm test` — fixture returns documented shapes; 503 maps correctly)
- [x] **T10 — Tool registry + direct invoke** — `src/registry.ts`: `registry.list()` (GET `/apis/erpbridge.io/v1/tools`), `registry.apply(def)` (POST, expects 201, maps 422 admission errors — the server's admission controller answers `invalid tool: ...` with 422, not 400), `registry.delete(name, version, {hard?})` (DELETE); `invoke(name, args)` → `ToolResult` (POST `/api/tools/invoke`, maps 404/500). **Note (C3):** `client.registry.list()` (REST registry) and `client.mcp.listTools()` (MCP protocol) are distinct surfaces returning different shapes. Registry CRUD deliberately lives outside `client.tools` so exact registered proxy names such as `list`, `apply`, or `delete` are never shadowed; document both in T11b.
      (**Seam:** T4 http core; **Files:** src/registry.ts, src/registry.test.ts; **Verify:** `npm test` — full CRUD + invoke against fixture; admission-error mapping; `npm run build`)
- [x] **T11a — Public API assembly** — `src/index.ts` exports `createClient(config): ErpbridgeClient` exposing `.mcp` (connect/listTools/callTool), `.tools` (exact-name MCP proxy only), `.registry` (REST registry CRUD), `.invoke`, `.logs`, `.metrics`, `.health`, `.cache`, `.close()`; subpath entry files `src/client.ts` (`@erpbridge/sdk/client`), `src/rest.ts` (`@erpbridge/sdk/rest`), `src/types.ts` (`@erpbridge/sdk/types`); JSDoc on every public symbol. Add `src/index.test.ts` using the fixture server to construct the public facade and exercise representative MCP and REST capabilities through it. No auth params beyond the inert `token`/`tokenEnv` config fields (D17).
      (**Seam:** n/a — assembly; **Files:** src/index.ts, src/index.test.ts, src/client.ts, src/rest.ts; **Verify:** `npm test && npm run build && npm pack --dry-run` — public facade and exports resolve)
- [x] **T11b — README** — README with install + 5-line quickstart (`createClient({ baseUrl })` → `await client.tools.list_employees({})`), error-handling section, and the C3 note distinguishing `registry.list()` (registry REST) from `mcp.listTools()` (MCP protocol). No auth section in v1 — link to the future auth plan instead. Pair this README update with the matching `CHANGELOG.md` Unreleased and `erpbridge-docs` updates required by the documentation-pairing rule.
      (**Seam:** n/a — writing; **Files:** README.md, CHANGELOG.md; **Verify:** `npm test && npm run build && npm run lint:publish` green; README renders on npmjs package page preview; `npm run build` green in erpbridge-docs)
- [x] **T12 — Packed-consumer smoke milestone (R5)** — after T11b and T1a, run `npm run build && npm run lint:publish && npm pack`, install the tarball into a scratch project and smoke-test `createClient` + `mcp.listTools()` + `logs.recent()`; **also import every subpath (`@erpbridge/sdk/client`, `/rest`, `/types`) to verify the exports map resolves in a real consumer** (catches the class of bug the MCP SDK v1.29.0 had). This is an internal package-shape check: do not change the package version, create a versioned CHANGELOG entry, tag, create a GitHub release, or publish to npm.
      (**Seam:** n/a — milestone; **Files:** scratch smoke script only (uncommitted); **Verify:** `npm test && npm run build && npm run lint:publish`; packed tarball installs in scratch project; smoke script runs against fixture and imports all subpaths)
- [x] **T13 — Live integration suite** — `tests/integration/` gated behind the `ERPBridge_TEST_SERVER` env var (skips otherwise). Three sub-files:

      **`integration.test.ts`** — happy paths:
      1. `health()` → `{ status: "ok" }`
      2. `mcp.connect()` + `mcp.listTools()` → includes the built-in tools (`system.progress_test`, `system.sensitive_log_test`)
      3. `mcp.callTool("system.progress_test", {})` → `ToolResult`
      4. `tools.<tool>(args)` via proxy (requires seeded tools)
      5. `invoke("system.progress_test", {})` → `ToolResult` via REST
      6. `logs.recent()` → non-empty `LogRecord[]` after tool calls
      7. `logs.stream()` → emits ≥1 record within 10s after a tool call, then aborts cleanly
      8. `metrics.text()` → contains `mcp_tool_invocations_total`
      9. `metrics.parsed()` → families include counters + histograms
      10. `cache.stats()` → `CacheStats` (redis) or `ServerError` (no redis)
      11. `registry.list()` (registry REST) → array with seeded tools
      12. `registry.apply()` + `registry.delete()` → registry CRUD round-trip

      **`integration-errors.test.ts`** — error paths:
      1. `mcp.callTool("nonexistent_tool", {})` → `NotFoundError`
      2. `invoke("nonexistent_tool", {})` → mapped error
      3. `cache.flush()` without redis → `ServerError` (503)
      4. real 404/500 where triggerable — verified at T13 time: `system.sensitive_log_test` does **not** 500 (logs + returns success); no built-in tool 500s, so no live 500 trigger exists yet — documented in the runbook

      **`tests/integration/README.md`** — runbook: prerequisites (Docker or Go + Python/uv); server startup via Docker Compose (recommended) — `docker compose up -d --build` in the ERPBridge repo, poll `/mcp/health` for readiness, `make generate-tools` to seed tools; health-check polling before the run; full command `ERPBridge_TEST_SERVER=http://localhost:8080 npm run test:integration`; teardown `docker compose down -v`; why integration tests are not in CI yet (future path: Docker Compose service in the CI workflow or a dedicated `integration.yml`).

      (**Seam:** real server via `docker compose up -d --build` in ERPBridge repo; **Files:** tests/integration/integration.test.ts, tests/integration/integration-errors.test.ts, tests/integration/README.md; **Verify:** `ERPBridge_TEST_SERVER=http://localhost:8080 npm run test:integration` green against the Docker Compose stack with seeded tools; reports saved to `.scratch/testing/` per Plan-testing.md)

- [x] **T14 — Docs-site completeness and release validation** — in **erpbridge-docs** repo (separate repo, `docs/sdk/`): consolidate and cross-link the documentation updates paired with T2–T11b; replace stub `overview.mdx` with a real landing page; add `installation.mdx`, `quickstart.mdx` (Tabs for npm/pnpm), `mcp-tools.mdx`, `logs-metrics.mdx`; update `docs/erpbridge/intro.mdx` components table + `docs/roadmap/overview.mdx` Phase 3 to list the SDK. The agent guide page (`docs/sdk/agent-guide.mdx`) already exists and mirrors this repo's AGENTS.md. `authentication.mdx` is deferred to the future auth plan.
      (**Seam:** docs/sdk/ already in sidebar/navbar (sidebars.ts:80, docusaurus.config.ts:136-141); **Files:** docs/sdk/*.mdx, docs/erpbridge/intro.mdx, docs/roadmap/overview.mdx, CHANGELOG.md Unreleased; **Verify:** `npm test && npm run build && npm run lint:publish` green in this repo; `npm run build` green in erpbridge-docs — fails on broken links/MDX)
- [x] **T15 — CI workflow** — `.github/workflows/ci.yml`: Node LTS matrix (`lts/-2`, `lts/-1`, `lts/*`), `npm ci`, jobs for typecheck / lint / test / build / publish-gate; top-level `permissions: contents: read`; SHA-pinned actions (checkout + setup-node with comments); `npm audit --audit-level=high` + `google/osv-scanner-action` PR scan; `npm pack --dry-run --json` assertions. _(RF10 note: no separate ESLint in v0.1.x — the gate is `typecheck` + `build` + `publish-gate` (publint/attw/audit) + osv scans.)_
      (**Seam:** n/a — repository automation; **Files:** .github/workflows/ci.yml, .github/dependabot.yml; **Verify:** `npm test && npm run build && npm run lint:publish`; workflow passes on a local push; gates fail on a deliberately broken pack)
- [x] **T16 — API docs workflow** _(superseded: typedoc dropped in favor of the hand-written erpbridge-docs site — see commit history)_ — `.github/workflows/docs.yml` + `typedoc.json`: build TypeDoc from `src/` into `docs/api/` on push to `main` and release tags; deploy via `actions/configure-pages` → `actions/upload-pages-artifact` → `actions/deploy-pages` with `permissions: pages: write` + `id-token: write` on the `github-pages` environment.
      (**Seam:** n/a — repository automation; **Files:** .github/workflows/docs.yml, typedoc.json; **Verify:** `npm test && npm run build && npm run lint:publish`; `npm run docs` emits `docs/api/`; Pages deploy succeeds after enabling Pages > Source: GitHub Actions)
- [x] **T17 — npm Trusted Publishing bootstrap** — before the first automated release, an authorized package maintainer creates `@erpbridge/sdk` on npm through a one-time local publish with a narrowly scoped, short-lived granular automation token, configures the repository/workflow as npm's trusted publisher, then revokes the token. Do not place that token in repository files, CI secrets, logs, or test reports. This is an operator-run prerequisite, not a CI workflow step.
      (**Seam:** npm registry; **Files:** none; **Verify:** npm package settings show the trusted publisher; a subsequent CI OIDC dry-run/controlled release is authorized without `NODE_AUTH_TOKEN` — completed: `@erpbridge/sdk@0.1.0` published via OIDC with sigstore provenance)
- [x] **T18 — Release workflow** — `.github/workflows/release.yml`: job 1 runs `googleapis/release-please-action@v4` (outputs `release_created`); job 2 (needs: release) publishes with npm **trusted publishing (OIDC)** — `permissions: id-token: write`, no `NODE_AUTH_TOKEN`, `npm ci && npm run build && npm run lint:publish && npm publish`, no caching; job 3 creates the GitHub Release notes. **Every release updates erpbridge-docs** (`docs/sdk/` pages + agent-guide sync + CHANGELOG Unreleased) — open/merge that docs PR in the same release cycle. Requires T16 and T17, a public repo, and a `repository.url` matching the GitHub repo (provenance requirement). _(RF10 note: "job 3 creates the GitHub Release notes" is folded into release-please-action's job 1 — functionally equivalent.)_
      (**Seam:** n/a — repository automation; **Files:** .github/workflows/release.yml; **Verify:** release PR opens on a `feat:` commit; after T17, merge publishes `0.1.0` with provenance; docs PR merged in the same cycle; Pages workflow green; `npm audit`/osv clean)

## Review Fixes (2026-08-20 code review)

Two-axis review of `2e4d7b15...HEAD` (report: `.scratch/summaries/2026-08-20T213643Z_review_v0.1.0-full_standards-spec.md`). Tasks below fix the actionable findings; each is one commit with its Verify line green.

- [x] **RF1 — Fix the AGENTS.md workflow contract** — AGENTS.md says `.github/workflows/{ci,release,docs}.yml` must stay green, but `docs.yml` was deleted with typedoc (`0bc17ed`). Rewrite the rule to `{ci,release}.yml` and note the docs-site is the API reference home. Mirror the change in `erpbridge-docs/docs/sdk/agent-guide.mdx` (kept in sync per AGENTS.md).
      (**Verify:** `git diff` shows only AGENTS.md here; docs build green in erpbridge-docs)
- [x] **RF2 — Repair the CHANGELOG layout** — the 0.1.0 release-please run left the file malformed: `## [0.1.0]` above the `# Changelog` title, a duplicate `## Changelog` heading + intro below it, and Unreleased content orphaned under a released version. Restructure to Keep a Changelog: single title, `## [Unreleased]` (only the typedoc Removed entry), then `## [0.1.0]` with the generated Features verbatim. Never hand-edit the 0.1.0 entry's content — only its position.
      (**Verify:** first lines are `# Changelog` → `## [Unreleased]` → `### Removed` → `## [0.1.0]`; no duplicate headings; `npm test` green)
- [x] **RF3 — Add `ClientError` for 4xx (D7 fix)** — `mapHttpError` routes non-401/404/429 4xx (400, 422) into `ServerError`, contradicting D7's "ServerError (5xx)" and README silently retcons the hierarchy. Add `ClientError extends ErpbridgeError` (HTTP 4xx) and map 4xx → `ClientError`, 5xx → `ServerError`. Update registry JSDoc (422 admission → `ClientError`), AGENTS.md hierarchy line, README error section, CHANGELOG Unreleased (Added `ClientError`, Changed mapping), and erpbridge-docs `api-reference.mdx` + `agent-guide.mdx`. TDD: extend `http.test.ts` with 400/422 → `ClientError` and 500 → `ServerError` assertions; update `registry.test.ts` 422 assertion.
      (**Verify:** `npm test && npm run typecheck && npm run build` green; docs build green in erpbridge-docs)
- [x] **RF4 — Nightly vulnerability scan (D15)** — D15 requires audit + osv on PRs **and nightly**; only PR/push coverage exists. Add `schedule: cron '0 3 * * *'` to ci.yml; osv-scan runs the PR reusable workflow on pull_request and `google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@<pinned-sha>` (`scan-args: -r ./`, `fail-on-vuln: true`, same permissions) on schedule; `npm audit` runs via publish-gate (schedule triggers all jobs). _(Post-review note: the PR job also passes `fail-on-vuln: true` — kept, it matches D15's fail-the-release intent.)_
      (**Verify:** `actionlint` clean (if installed) or YAML parse; `npm test` unaffected)
- [x] **RF5 — Extract shared fixture helpers** — `readBody`, `respond`, and the socket-track/close boilerplate are duplicated across `fixtures/http-server.ts`, `fixtures/mcp-server.ts`, `fixtures/logs-sse.ts`. Extract `fixtures/helpers.ts` and refactor all three.
      (**Verify:** `npm test` green — all fixture-based suites still pass)
- [x] **RF6 — Shared protocol-error module** — `INTERNAL_ERROR_CODE = -32000` and the "validate shape → throw ProtocolError" idiom are duplicated in logs/registry/system/mcp. Extract `src/errors.ts` (`INTERNAL_ERROR_CODE` + `protocolShapeError(endpoint, expectation)`) and refactor the four modules. _(Post-review note: implemented as `protocolError(message, cause?)` — construction is shared; each call site keeps its own expectation message, which reads better than a two-arg shape helper.)_
      (**Verify:** `npm test && npm run typecheck && npm run build` green)
- [x] **RF7 — Precise unknown-tool mapping** — `mapMcpError` uses `message.includes('not found')` (AGENTS.md forbids string-matching). mcp-go's miss format is `tool '<name>' not found: tool not found` — narrow the match to `code === INVALID_PARAMS_CODE && message.includes("'<name>' not found")` via a named `isUnknownToolError(name, code, message)`. TDD: unknown tool still maps to `NotFoundError`; an error naming a *different* tool stays `ProtocolError`. CHANGELOG entry. _(Post-review note: shipped as `isUnknownToolError(message, toolName, code)` — same logic, argument order follows use.)_
      (**Verify:** `npm test` green)
- [x] **RF8 — Single-source MCP client version + rename smells** — `CLIENT_VERSION = '0.1.0'` (mcp.ts) and the spike script duplicate package.json's version; release-please only bumps package.json, so the MCP client-info would advertise a stale version. Read `package.json` at runtime via `createRequire(import.meta.url)` in mcp.ts and via `JSON.parse` in `scripts/mcp-compat.mjs`. Rename private `McpClient.run()` → `execute()` and `handle()` → `proxyFor()` (naming). CHANGELOG entry (fix: client-info version). _(Post-review hardening (`496389f`): resolution wrapped in try/catch with a `'0.0.0'` fallback so exotic bundling cannot break module load; a fixture-captured handshake test asserts the advertised version equals package.json.)_
      (**Verify:** `npm test && npm run build` green; `node -e "require('./dist/client.cjs')"` and `node -e "import('./dist/client.mjs')"` both load — mcp.ts compiles into the `client` entry, there is no `dist/mcp.*`)
- [x] **RF9 — Prometheus type narrowing** — `flush()` casts `current.type as MetricFamily['type']`, hiding the SUPPORTED_TYPES invariant. Introduce a `FamilyType` union + `isSupportedType` guard; drop the cast. **Verify:** `npm test` green.
- [x] **RF10 — Plan notes** — amend D7 (ClientError added), D15 (nightly scan implemented), T8 (skip-and-report interpretation, not reject), T15 (no separate linter in v0.1.x — typecheck + tsdown lint + actionlint cover the gate), T18 (release notes fold into release-please-action job 1). **Verify:** plan reads consistently.
- [x] **RF11 — Unplanned MCP robustness fix, backfilled tests** — commit `90d07ae` fixed two real findings from the originating report without an RF task: `execute()` no longer treats a caller abort (`AbortError`) as a transport failure (it propagates instead of reconnect + retry), and `connect()` closes the half-built `Client` when the handshake fails. Recorded here because it violated two rules as shipped: it landed with **no test** and bundled two behaviors in one commit (the split is historical; both belong to the same review-followup effort). Follow-ups:
      - `3fea7a4` — test: aborted `tools/call` propagates `{ name: 'AbortError' }` with `handshakeCount()` unchanged (verified red against the pre-fix logic). The close-on-connect-failure half stays untested deliberately: it has no observable contract without spying on the MCP SDK `Client`, which D9's no-mock rule forbids.
      - `b8fc4cc` — fixture: `startMcpFixture` gains `unknownToolError` override + `lastClientInfo()` capture; the mismatched unknown-tool test now uses the shared fixture instead of hand-rolling a server.
      - `496389f` — hardening: `resolveClientVersion()` falls back to `'0.0.0'` instead of crashing module load when package.json is unreadable; advertised-version handshake test added.
      (**Verify:** `npm test && npm run typecheck && npm run build` green; 103 tests)

## Verification

1. Every implementation task follows the TDD and per-task execution sequence in `Plan-testing.md`: focused expected-red/green test, task-specific verification, then `npm test && npm run build`; package artifacts additionally run `npm run lint:publish`.
2. `npm run build` produces valid ESM + CJS `dist/`; `npm pack --dry-run` contains `dist/`, `src/`, README, package.json — and nothing else.
3. **Package shape**: `npm run lint:publish` (publint + attw --pack) green against the tarball — exports resolve under `node10`/`node16`/`bundler`; `require` branch resolves to CJS with `.d.cts`; subpaths `./client`, `./rest`, `./types`, `./package.json` resolve.
4. **MCP**: T5 live compatibility spike green first (initialize + tools/list against the real server, v2 client ↔ mcp-go 2025-11-25); `createClient({ baseUrl }).mcp.listTools()` returns the 14 schemas' names; `client.tools.list_employees({})` executes; fixture-defined MCP unknown tool throws `NotFoundError`; recognized tool failures return `ToolResult { isError: true }`; reconnect re-initializes once then throws `ProtocolError`.
5. **REST**: `logs.recent()`, `logs.stream()` (SSE), `metrics.parsed()` (counters + gauges + histograms, summaries rejected), `health()`, `cache.stats()`, `registry.apply/list/delete`, `invoke()` all round-trip against the fixture and (gated) the live server.
6. **Packed consumer**: the current workspace tarball installs into a scratch project, imports all public subpaths, and its smoke script runs against the fixture without a manual version/changelog/tag/release change.
7. **CI**: workflow green on the Node LTS matrix; publish-gate job fails on a broken tarball; audit + osv scans clean.
8. **Release**: after the documented trusted-publishing bootstrap, a `feat:` commit on `main` opens a release PR; merge publishes `0.1.0` to npm with provenance; tag + GitHub Release created; the already-configured Pages workflow deploys; docs-site PR is merged in the same release cycle.
9. **Docs**: erpbridge-docs `npm run build` green with the new SDK pages; llms.txt artifacts regenerate.
10. **Auth deferred**: v1 ships with no token handling; server 401s surface as `AuthenticationError`. Auth behavior is verified under the future `Plan-auth.md`, not here.
11. **Test reporting**: every meaningful test run saves a summarized agent-readable report to `.scratch/testing/` per `Plan-testing.md`; unexpected failures and flakes are always saved and trigger a testing-plan amendment before the next task starts.

## Open Questions

- **npm publishing bootstrap**: trusted publishing (OIDC) requires the package to already exist on npmjs.com. T17 is the explicit operator-run bootstrap prerequisite; do not bypass it or retain its temporary granular token after configuration.
- **MCP version alignment (R1, settled)**: `@modelcontextprotocol/client@~2.0.0` is pinned because its protocol set (`2025-11-25` latest, negotiable to `2025-03-26`) matches the server's mcp-go v0.57.0. The only residual risk is the v2 client's OAuth metadata probe on the server that hosts no `/.well-known/oauth-protected-resource` — verified during the T5 spike. Fallback to `@modelcontextprotocol/sdk@^1.30.0` only if the spike fails.
- **Server MCP upgrade (tracking)**: ERPBridge's `Plan-MCP-Upgrade.md` (future) migrates to mcp-go v1.0.0-beta.1 / spec `2026-07-28`. When that lands, re-evaluate the SDK's client pin (D2). Not blocking v1.
- **Server auth (tracking)**: ERPBridge's `Plan-Auth.md` (A1–A10) is unimplemented. SDK auth starts when it ships — see `.agents/plans/Plan-auth.md` (future).
- **Prefix normalization**: server registers bare tool names today; if the server later adds the `erp.` prefix (AGENTS.md says it), the proxy key strategy in D6 may need a normalization option. Tracked, not blocking v1.
- **tools proxy staleness (T6 follow-up)**: the proxy's discovered tool list is cached for the client's lifetime; it is not invalidated on `notifications/tools/list_changed`. Tracked for a post-v1 hardening task, not blocking v1.

## Browser-compatible MCP/tools (post-v0.1.0)

### Goal

Let modern browser applications bundle and use the existing MCP client and
exact-name `client.tools` proxy without Node built-in polyfills, while keeping
the current Node >=20 ESM and CJS consumer contract unchanged. Browser support
is limited to MCP/tools; REST APIs remain unsupported for cross-origin browser
use in this release.

### Current State

- `src/mcp.ts` statically imports `createRequire` from `node:module` and calls
  it while evaluating `CLIENT_VERSION` (`src/mcp.ts:4-23`). The generated ESM
  client chunk preserves that import (`dist/client-D-OMAqwn.mjs:3`), so a
  browser bundler must resolve a Node-only module even when the package-version
  lookup would fall back at runtime.
- The root and `./client` entries load that MCP chunk (`src/index.ts:1,7`; the
  `./rest` entry does not), while the package already publishes ESM and CJS
  branches (`package.json:31-69`).
- `@modelcontextprotocol/client@2.0.0` has its own browser-aware conditional
  shim (`node_modules/@modelcontextprotocol/client/package.json:74-104`) and
  its root client code imports no `node:*` modules. It remains external from
  this package by design (`tsdown.config.ts:15-17`).
- ERPBridge already permits browser MCP origins and exposes `Mcp-Session-Id`,
  but its preflight allowlist omits the required `MCP-Protocol-Version` request
  header (`../ERPBridge/internal/mcp/server.go:469-473`). The MCP Streamable
  HTTP spec requires this header on subsequent requests, and browsers require
  the session header to be CORS-exposed ([MCP transport spec](https://modelcontextprotocol.io/specification/draft/basic/transports), [MCP browser CORS guidance](https://py.sdk.modelcontextprotocol.io/run/asgi/)).
- Conditional exports should select distinct artifacts only where runtime
  behavior differs; a universal `default` branch is preferred to a redundant
  browser branch ([Node package documentation](https://nodejs.org/api/packages.html)).

### Decisions

| # | Decision | Rationale / rejected |
| --- | --- | --- |
| B1 | **Browser contract:** modern browsers with native `fetch`, `URL`, `AbortSignal`, readable streams, and Web Crypto may import the package ESM build and use only `client.mcp`, `client.tools`, and `client.close()`. Node >=20 ESM/CJS support and all existing public APIs remain unchanged. | Matches the requested MCP/tools-only scope. REST endpoints receive no new CORS promise and must use Node, a same-origin deployment, or an application proxy. |
| B2 | **One universal MCP implementation:** remove every Node built-in import from shipped MCP source; inject the SDK version at build/test-config time rather than reading `package.json` at runtime. Keep the version accurate in the MCP `initialize` client-info. | The current `try/catch` cannot save a static `node:module` import from browser resolution. A Node-only MCP entry or a browser-only facade would duplicate behavior without a present runtime difference. |
| B3 | **Package shape:** preserve the existing root, `./client`, `./rest`, `./types`, ESM, CJS, declaration, and legacy fallback exports. Emit the shared ESM code with a neutral/ES2022-compatible build target; do not add a `browser` condition that points to the same artifact. | Node retains the tested CJS branch; browser bundlers consume ESM. Environment conditions are reserved for future truly divergent artifacts. |
| B4 | **Server boundary:** change only `/mcp/` CORS to permit `MCP-Protocol-Version`, retaining the existing origin/method/header policy and exposed `Mcp-Session-Id`. Do not add global REST CORS. | Makes the supported browser MCP flow work without exposing management, logs, metrics, cache, registry, or direct-invoke endpoints cross-origin. |
| B5 | **Compatibility proof:** use Vite as an explicit development dependency and bundle a packed-package consumer that imports both the root facade and `@erpbridge/sdk/client`; separately retain Node ESM/CJS load checks and add an ERPBridge preflight test. | A source-level search is insufficient: the regression is a consumer-bundler resolution failure. Vite exercises the installed package's ESM/conditional dependency path. |

### Tasks

- [x] **B1 — Make MCP version metadata browser-safe (one SDK commit).** Start TDD by extending `src/mcp.test.ts` to prove the handshake still advertises the package version after removing runtime manifest loading. Add an internal version module or build-time global that is replaced by tsdown and Vitest configuration with the value from `package.json`; remove `createRequire`/`node:module` from `src/mcp.ts` and all published SDK source. Keep this value internal—no new public API export. Configure the ESM build as neutral with an ES2022-compatible target, preserving current CJS output and external MCP dependency. Update `README.md`, `CHANGELOG.md` Unreleased, and the paired `erpbridge-docs` SDK installation/overview/MCP-tools pages to state the Node and browser MCP/tools contract, browser prerequisites, and the explicit REST limitation. (**Seam:** `McpClient.connect()` → fixture-captured `clientInfo`; emitted ESM artifact; **Files:** `src/mcp.ts`, new internal version module if needed, `src/mcp.test.ts`, `tsdown.config.ts`, `vitest.config.ts`, `README.md`, `CHANGELOG.md`, `../erpbridge-docs/docs/sdk/{installation,overview,mcp-tools}.mdx`; **Verify:** focused MCP test is red before the change, then `npm test && npm run typecheck && npm run build`, `node -e "require('./dist/client.cjs')"`, and `node -e "import('./dist/client.mjs')"` are green; `rg 'node:module|createRequire' src dist` has no published-code matches.)

- [x] **B2 — Add a packed browser-bundle regression gate (same SDK commit only if it is needed to make B1 green; otherwise one `test:` commit).** Add Vite as a direct development dependency and a `test:browser-bundle` script. Its hermetic temporary consumer must install the current packed tarball, production-bundle one entry importing `createClient` from the root and another importing the public `createClient` from `@erpbridge/sdk/client`, and fail if bundling fails or output retains `node:module`/`createRequire`. Run it in CI after the normal build; do not run a live browser or call a real server from this gate. Preserve `npm test` as the fast fixture suite. (**Seam:** real package `exports` resolution in Vite's browser dependency pipeline; **Files:** `package.json`, `package-lock.json`, new `scripts/assert-browser-bundle.mjs`, `.github/workflows/ci.yml`; **Verify:** the test fails against the current artifact, then `npm run build && npm run test:browser-bundle && npm test && npm run typecheck` is green, followed by `npm run lint:publish`.)

- [x] **B3 — Align ERPBridge MCP CORS with browser protocol requests (one ERPBridge `fix:` commit).** Add `MCP-Protocol-Version` to the existing Streamable HTTP CORS allowed headers without changing origins, methods, credentials, REST handlers, or auth behavior. Extend the server's HTTP tests with an `OPTIONS /mcp/` cross-origin preflight requesting `Content-Type`, `Mcp-Session-Id`, and `MCP-Protocol-Version`; assert the allowed headers include all three and `Mcp-Session-Id` remains exposed on the initialize response. Update the server connectivity guide’s browser statement to name the required protocol/session behavior. (**Seam:** `Server.ServeHTTP()` Streamable HTTP CORS middleware; **Files:** `../ERPBridge/internal/mcp/server.go`, `../ERPBridge/internal/mcp/server_test.go`, `../ERPBridge/docs/connectivity.md`; **Verify:** focused Go CORS test is red first, then `go test ./internal/mcp/...` and the ERPBridge repository’s standard test/build gates are green.)

- [x] **B4 — Release-level consumer verification and documentation review (one SDK `docs:`/`test:` commit only if B1-B2 did not already contain the required docs).** Run the packed Node consumer smoke plus the new Vite smoke against the release candidate. Confirm the docs distinguish browser MCP/tools from Node-only/cross-origin-unsupported REST calls and describe the active opt-in, consume-only auth contract. Record the new browser scenario in `.scratch/testing/` only when it reveals a new environment fact, failure, or flake; do not commit scratch content. (**Seam:** packed tarball installed by independent Node and browser consumers; **Files:** documentation only if corrections are needed; **Verify:** `npm test && npm run typecheck && npm run build && npm run test:browser-bundle && npm run lint:publish`; Node packed-consumer smoke remains green; docs-site `npm run build` is green in `../erpbridge-docs`.)

### Verification

1. The ESM artifacts reachable from `@erpbridge/sdk` and `@erpbridge/sdk/client` contain no Node built-in import or `createRequire` call; the CJS artifacts still load under Node.
2. The MCP fixture captures the exact package version in the initialize handshake for source tests and the packed Node consumer.
3. A Vite production build of a consumer installed from the tarball succeeds for both supported browser import paths without a Node polyfill.
4. ERPBridge accepts a browser-style MCP preflight containing `MCP-Protocol-Version`, returns/exposes `Mcp-Session-Id`, and preserves the existing MCP behavior. No REST route gains cross-origin access.
5. README and SDK-site docs state the support matrix precisely: browser = MCP/tools only; Node >=20 = full SDK; browser deployment needs ERPBridge MCP CORS; v1 still has no auth handling.

### Assumptions

- “Browser” means a current evergreen browser used through an ESM-capable bundler such as Vite, not direct unbundled npm imports, legacy browsers, React Native, Deno, Workers, or SSR-specific behavior.
- Browser requests target an ERPBridge instance using the MCP `/mcp/` Streamable HTTP endpoint. A frontend must still use a safe deployment origin policy; this task preserves the server’s existing `*` policy rather than designing a new origin configuration system.
- REST browser support, browser credential storage, and global CORS are explicitly deferred; the active compatibility plan covers SDK bearer injection while the server remains the authority for browser deployment policy.

---

# ERPBridge v0.3.0-alpha.1 compatibility upgrade

**Status:** complete — R1 through R8 verified; live auth coverage remains opt-in.

## Goal and release boundary

Upgrade `@erpbridge/sdk` from `0.1.1` to work with ERPBridge
**v0.3.0-alpha.1** (release commit `083acc4`, 2026-08-22). Preserve anonymous
open-mode operation, while adding consumer-side bearer credentials, role-aware
invocation, the new registry contract, and correct MCP result handling.

This is **consume-only**: the SDK sends opaque credentials supplied by the
application but does not create, list, reveal, revoke, refresh, or store
tokens. The server's release already uses an MCP protocol range compatible with
the installed `@modelcontextprotocol/client@2.0.0`; do not change MCP client
dependencies as part of this work.

Use focused `feat:` commits so release-please creates the next pre-1.0 minor
release (expected `0.2.0` from `0.1.1`). Do not hand-edit package versions or
versioned CHANGELOG entries. The MCP return type changes materially, so include
an upgrade note; do not add a `BREAKING CHANGE:` footer unless maintainers
explicitly choose a major release.

## Release facts and decisions

| Server v0.3.0-alpha.1 contract | SDK compatibility decision |
| --- | --- |
| Auth is opt-in: an unset `API_AUTH_TOKEN` leaves the server open. `/mcp/`, `/metrics`, and `/api/logs/*` respectively require `mcp`, `metrics`, and `logs` scopes when enabled. Registry, cache, and direct invoke require admin; health stays open. | No resolved token means silent anonymous operation. Global credentials cover every surface; only MCP, metrics, and logs allow a per-surface override. The SDK never treats configuration as proof that a token is valid or privileged. |
| The server authenticates from `Authorization`; current 401/403 responses do not consistently include `WWW-Authenticate`. | Preserve a received challenge without logging it. Leave `requiredScope` unset unless an actual bearer insufficient-scope challenge provides it—never infer scopes from an endpoint, body, or opaque token. |
| MCP `tools/call` returns protocol `CallToolResult` envelopes; REST direct invoke still returns `{ result, error?, isError? }`. | Export distinct `McpToolResult` and REST `ToolResult` types. Stop parsing JSON text and flattening MCP content. |
| Registry supports exact `name`/`version` filters and MCPTool manifests with `spec.security.allowedRoles`; roles differ by transport. | Add typed filters and manifests. MCP callers pass `role` in tool arguments; direct REST invoke sends only `X-ERPBridge-Role`, never `arguments.role`. |
| Cache falls back to in-process LRU when Redis is absent; the existing stats shape can have empty `redisMemory`. | Keep cache APIs/types; treat a successful memory-mode stats response as healthy rather than expecting 503. |

Evidence: `../ERPBridge/CHANGELOG.md`, `docs/{api,tokens,tool-schema,caching}.md`, route/auth code in `internal/mcp/{server,auth,authz,tool}.go`, and installed MCP client v2 transport/error declarations.

## API contract to implement

### Credential input, resolution, and routing

Activate the existing additive fields and expose these public types:

```ts
type AuthScope = "mcp" | "metrics" | "logs";

interface SurfaceAuth {
  token?: string;
  tokenEnv?: string;
  declaredScopes?: readonly AuthScope[];
}

interface ErpbridgeClientConfig {
  token?: string;
  tokenEnv?: string;
  declaredScopes?: readonly AuthScope[];
  auth?: { mcp?: SurfaceAuth; metrics?: SurfaceAuth; logs?: SurfaceAuth };
}
```

Resolve credentials once while normalizing `createClient` configuration. Empty
strings are absent. `tokenEnv` is an environment-variable *name*, never a
token. Read `globalThis.process?.env` without statically importing `node:*` so
browser bundles remain valid; browsers get no env-derived token but can use an
explicit `token`.

For each protected surface use this precedence:

1. surface `token`;
2. surface `tokenEnv` value, if non-empty;
3. global `token`;
4. global `tokenEnv` value, defaulting the name to `ERPBRIDGE_TOKEN` when it
   was omitted; if that canonical variable is absent, read the legacy
   `ERPBridge_TOKEN` variable for backward compatibility;
5. anonymous access.

`declaredScopes` follows the winning credential level. It is only a caller
assertion. Before MCP/metrics/logs I/O, throw a local `AuthorizationError` only
if a non-empty declaration excludes that surface's scope. With no declaration,
send the request. Do not local-guard health/cache/registry/direct invoke: their
admin policy is outside this three-scope model.

When a token resolves, generated `Authorization: Bearer <token>` overrides any
caller-provided Authorization header; with no token preserve caller headers.
Never include bearer or `WWW-Authenticate` values in error messages, test
output, logs, scratch reports, or examples.

### Results, errors, registry, and cache

- Retain `ToolResult` for direct REST invoke and broaden `error` to `unknown`.
- Export `McpToolResult` as the official client `CallToolResult`; `mcp.callTool`
  and `client.tools.<bareName>()` return it unchanged, including `content`,
  `structuredContent`, and `isError`.
- Add exported `AuthorizationError extends ErpbridgeError` for 403, with status,
  body, and optional `requiredScope`. Keep `AuthenticationError` for 401 and
  add `wwwAuthenticate?` while retaining `hint` for compatibility.
- Map REST 401/403/404/429/other 4xx/5xx to Authentication/Authorization/
  NotFound/RateLimit/Client/Server errors. Map MCP `UnauthorizedError` to
  AuthenticationError and `InsufficientScopeError` or 403 `SdkHttpError` to
  AuthorizationError before generic reconnect handling. Auth failures never
  reconnect.
- Add `registry.list({ name?: string; version?: string })` and
  `registry.invoke(name, arguments, { role?: string })`. Model
  `apiVersion: "erpbridge.io/v1"`, `kind: "MCPTool"`, and
  `spec.security.allowedRoles?: readonly string[]`.

## Tasks

Each checkbox is one focused Conventional Commit. Follow TDD: make a fixture
test fail first, implement the minimum, then refactor. Use `node:http` and MCP
fixtures, never fetch/MCP mocks. Read `Plan-testing.md` before meaningful test
runs; write ignored `.scratch/testing/` reports only for failures, flakes, or
new test/environment discoveries.

- [x] **R1 — Activate current guidance and public auth vocabulary (`docs:` then `feat:`).** Update `AGENTS.md` so auth is no longer prohibited and mark the former future plan historical. Add/export `AuthScope`, `SurfaceAuth`, normalized credential internals, `AuthorizationError`, and backward-compatible authentication challenge fields in `src/{types,config,index}.ts`. Test public exports, error shape, and browser-safe env access without static Node imports. Synchronize `../erpbridge-docs/docs/sdk/agent-guide.mdx`, but do not yet claim injection works. **Verify:** focused tests red then `npm test && npm run typecheck && npm run build && npm run lint:publish`; docs-site `npm run build`.

- [x] **R2 — Resolve and inject REST/SSE credentials (`feat:`).** Implement the exact precedence table in config, carry an internal surface identity through HTTP, and merge headers case-insensitively. Route metrics through `metrics`, both logs methods and all SSE reconnects through `logs`, and health/cache/registry/direct invoke through global auth. Extend HTTP/SSE fixtures to capture headers without displaying their values. Cover global explicit/default-env/custom-env, surface override, empty values, anonymous operation, caller Authorization precedence, and SSE reconnect. Update README, CHANGELOG Unreleased, and docs authentication/installation/overview with the formerly-inert-token migration. **Verify:** focused red then `npm test && npm run typecheck && npm run build && npm run lint:publish`; docs build.

- [x] **R3 — Add scope guards and REST error mapping (`feat:`).** Fail before I/O only for non-empty declared scopes that exclude MCP/metrics/logs; prove absent declarations reach the fixture and admin surfaces are never locally blocked. Map 403 to `AuthorizationError`; preserve challenge metadata only when actually present. Test 401, ordinary 403, challenge-bearing 403, 404, 429, and 5xx without leaking headers. Document scope declarations as assertions rather than token inspection. **Verify:** focused red then `npm test && npm run typecheck && npm run build && npm run lint:publish`; docs build.

- [x] **R4 — Authenticate MCP and map MCP auth failures (`feat:`).** Construct `StreamableHTTPClientTransport` with `requestInit.headers` and `onInsufficientScope: "throw"`; reuse resolved MCP auth for connect and reconnect. Map official `UnauthorizedError`, `InsufficientScopeError`, and 403 `SdkHttpError` before retry logic. Do not add OAuth/provider/refresh behavior. Extend the MCP fixture to validate initialize and subsequent headers and to return selected 401/403 responses; prove typed errors do not loop reconnect. **Verify:** focused red then `npm test && npm run typecheck && npm run build && npm run test:browser-bundle && npm run lint:publish`; docs build.

- [x] **R5 — Preserve MCP result envelopes (`feat:`).** First return text JSON, non-text content, `structuredContent`, and `isError` from the MCP fixture to expose today’s flattening. Remove MCP JSON-text parsing; return `McpToolResult` unchanged from direct and proxy calls. Keep REST direct-invoke `ToolResult` behavior. Add root and `./client` type-level consumer coverage and an upgrade example. **Verify:** focused red then `npm test && npm run typecheck && npm run build && npm run test:browser-bundle && npm run lint:publish`; docs build.

- [x] **R6 — Add registry/role contract support (`feat:`).** Add typed exact registry filters, MCPTool/allowed-role schema, and `DirectInvokeOptions.role` serialized solely in `X-ERPBridge-Role`. Fixture-test query encoding, absent options, manifest shape, header role, and absence of direct `arguments.role`. Separately prove guarded MCP calls preserve `role` as an ordinary MCP argument. Do not add local role validation. **Verify:** focused red then `npm test && npm run typecheck && npm run build && npm run lint:publish`; docs build.

- [x] **R7 — Align cache and opt-in integration proof (`test:` / `docs:`).** Make no-Redis fixture coverage return valid memory-mode stats (including empty `redisMemory`); retain 503 coverage only for a genuinely unavailable cache. Add `tests/integration/auth.test.ts`, skipped unless `ERPBridge_TEST_SERVER` and safely provisioned scoped/admin token variables exist. Cover scoped surfaces, admin registry/cache/direct invoke, 401/403 mapping, role-guarded calls, filters, and an MCP envelope. Tokens are provisioned externally with server tooling; never created, logged, or revealed by this SDK. Preserve anonymous integration testing with only `ERPBridge_TEST_SERVER`. **Verify:** fixture suite red then green; full package gates; safely provisioned `ERPBridge_TEST_SERVER=… npm run test:integration`; scratch report only for a discovery/failure/flake.

- [x] **R8 — Verify the packed release and documentation (`test:`/`docs:` only for corrections).** Pack the SDK and test clean Node ESM, Node CJS, and Vite MCP/tools consumers. Confirm root, `./client`, `./rest`, and `./types` exports; MCP remains external; browser output has no `node:*`; and type consumers distinguish `McpToolResult` from `ToolResult`. Review all SDK docs plus the agent guide for one consistent credential/error/result story. Leave release version entries to release-please. **Verify:** `npm test && npm run typecheck && npm run build && npm run test:browser-bundle && npm run lint:publish`, packed ESM/CJS smoke, docs-site `npm run build`.

- [x] **R9 — Normalize the default credential environment variable (`fix:`).** Use `ERPBRIDGE_TOKEN` as the canonical default environment variable, retain `ERPBridge_TOKEN` only as a fallback for existing consumers, and keep explicit `tokenEnv` precedence unchanged. Add fixture coverage for canonical-default selection and legacy fallback without exposing credential values. Update README, CHANGELOG Unreleased, and paired SDK authentication/API-reference/agent-guide docs with the migration note. **Verify:** focused red then `npm test && npm run typecheck && npm run build && npm run lint:publish`; docs-site `npm run build`.

## Completion criteria

1. Open-mode users remain anonymous and receive no generated bearer header.
2. Credential routing works across REST, SSE reconnects, MCP initialization, and MCP reconnects without secret disclosure.
3. 401 and 403 are independently catchable; ordinary 403 never fabricates `requiredScope`.
4. MCP envelopes are preserved and REST result compatibility remains intact.
5. Registry filters/schema, role routing, and memory-cache behavior match the release with hermetic and optional live coverage.
6. Package quality gates, packed consumers, and paired docs build pass; no generated artifacts or `.scratch` files are committed.

## Out of scope

Token lifecycle/admin credentials; OAuth, PKCE, refresh, or token storage;
MCP dependency/protocol upgrades; general REST browser CORS or browser credential
persistence; and SDK role-management or role validation.
